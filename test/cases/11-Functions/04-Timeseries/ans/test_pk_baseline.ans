
taos> USE test_pk_matrix;
Database changed.

taos> SELECT last(val) FROM t_base;
  last(val)  |
==============
          40 |

taos> SELECT first(val) FROM t_base;
 first(val)  |
==============
          30 |

taos> SELECT last_row(val) FROM t_base;
 last_row(val) |
================
            40 |

taos> SELECT tail(val, 2) FROM t_base;
 tail(val, 2) |
===============
           40 |
           20 |

taos> SELECT unique(val) FROM t_base;
 unique(val) |
==============
          30 |
          50 |
          20 |
          10 |
          40 |

taos> SELECT last(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
  last(val)  |
==============
          40 |

taos> SELECT first(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 first(val)  |
==============
          30 |

taos> SELECT last_row(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 last_row(val) |
================
            40 |

taos> SELECT tail(val, 2) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 tail(val, 2) |
===============
           40 |
           20 |

taos> SELECT unique(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 unique(val) |
==============
          30 |
          50 |
          20 |
          10 |
          40 |

taos> SELECT last(val) FROM t_dup;
  last(val)  |
==============
          30 |

taos> SELECT first(val) FROM t_dup;
 first(val)  |
==============
          10 |

taos> SELECT last_row(val) FROM t_dup;
 last_row(val) |
================
            30 |

taos> SELECT tail(val, 2) FROM t_dup;
 tail(val, 2) |
===============
           30 |
           25 |

taos> SELECT unique(val) FROM t_dup;
 unique(val) |
==============
          30 |
          20 |
          25 |
          10 |
          15 |

taos> SELECT elapsed(ts, 1s) FROM t_base;
      elapsed(ts, 1s)      |
============================
                         4 |

taos> SELECT elapsed(ts, 1s) FROM (SELECT * FROM t_base ORDER BY ts DESC);
      elapsed(ts, 1s)      |
============================
                         4 |

taos> SELECT elapsed(ts, 1s) FROM t_dup;
      elapsed(ts, 1s)      |
============================
                         2 |

taos> SELECT derivative(val, 1s, 0) FROM t_base;
  derivative(val, 1s, 0)   |
============================
                       -20 |
                        40 |
                       -30 |
                        20 |

taos> SELECT twa(val) FROM t_base;
         twa(val)          |
============================
                     28.75 |

taos> SELECT irate(val) FROM t_base;
        irate(val)         |
============================
                        20 |

taos> SELECT derivative(val, 1s, 0) FROM (SELECT * FROM t_base ORDER BY ts DESC);
  derivative(val, 1s, 0)   |
============================
                        20 |
                       -30 |
                        40 |
                       -20 |

taos> SELECT twa(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
         twa(val)          |
============================
                     28.75 |

taos> SELECT irate(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
        irate(val)         |
============================
                        20 |

taos> SELECT derivative(val, 1s, 0) FROM t_dup;
  derivative(val, 1s, 0)   |
============================
                        10 |
                        10 |

taos> SELECT twa(val) FROM t_dup;
         twa(val)          |
============================
                        20 |

taos> SELECT irate(val) FROM t_dup;
        irate(val)         |
============================
                        10 |

taos> SELECT diff(val) FROM t_base;
       diff(val)       |
========================
                   -20 |
                    40 |
                   -30 |
                    20 |

taos> SELECT csum(val) FROM t_base;
       csum(val)       |
========================
                    30 |
                    40 |
                    90 |
                   110 |
                   150 |

taos> SELECT mavg(val, 2) FROM t_base;
       mavg(val, 2)        |
============================
                        20 |
                        30 |
                        35 |
                        30 |

taos> SELECT statecount(val, 'GE', 30) FROM t_base;
 statecount(val, 'GE', 30) |
============================
                         1 |
                        -1 |
                         1 |
                        -1 |
                         1 |

taos> SELECT stateduration(val, 'GE', 30, 1s) FROM t_base;
 stateduration(val, 'GE', 30, 1s) |
===================================
                                0 |
                               -1 |
                                0 |
                               -1 |
                                0 |

taos> SELECT diff(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
       diff(val)       |
========================
                   -20 |
                    30 |
                   -40 |
                    20 |

taos> SELECT csum(val) FROM (SELECT * FROM t_base ORDER BY ts DESC);
       csum(val)       |
========================
                    40 |
                    60 |
                   110 |
                   120 |
                   150 |

taos> SELECT mavg(val, 2) FROM (SELECT * FROM t_base ORDER BY ts DESC);
       mavg(val, 2)        |
============================
                        30 |
                        35 |
                        30 |
                        20 |

taos> SELECT statecount(val, 'GE', 30) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 statecount(val, 'GE', 30) |
============================
                         1 |
                        -1 |
                         1 |
                        -1 |
                         1 |

taos> SELECT stateduration(val, 'GE', 30, 1s) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 stateduration(val, 'GE', 30, 1s) |
===================================
                                0 |
                               -1 |
                                0 |
                               -1 |
                                0 |

taos> SELECT diff(val) FROM t_dup;
       diff(val)       |
========================
                    10 |
                    10 |

taos> SELECT csum(val) FROM t_dup;

DB error: Duplicate timestamps not allowed in function [0x80002805] 
taos> SELECT mavg(val, 2) FROM t_dup;

DB error: Duplicate timestamps not allowed in function [0x80002805] 
taos> SELECT statecount(val, 'GE', 30) FROM t_dup;

DB error: Duplicate timestamps not allowed in function [0x80002805] 
taos> SELECT stateduration(val, 'GE', 30, 1s) FROM t_dup;

DB error: Duplicate timestamps not allowed in function [0x80002805] 
taos> SELECT lag(val, 1) FROM t_base;
 lag(val, 1) |
==============
 NULL        |
          30 |
          10 |
          50 |
          20 |

taos> SELECT lead(val, 1) FROM t_base;
 lead(val, 1) |
===============
           10 |
           50 |
           20 |
           40 |
 NULL         |

taos> SELECT fill_forward(val) FROM t_fill;
 fill_forward(val) |
====================
                10 |
                10 |
                10 |
                40 |
                40 |

taos> SELECT lag(val, 1) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 lag(val, 1) |
==============
 NULL        |
          40 |
          20 |
          50 |
          10 |

taos> SELECT lead(val, 1) FROM (SELECT * FROM t_base ORDER BY ts DESC);
 lead(val, 1) |
===============
           20 |
           50 |
           10 |
           30 |
 NULL         |

taos> SELECT fill_forward(val) FROM (SELECT * FROM t_fill ORDER BY ts DESC);
 fill_forward(val) |
====================
 NULL              |
                40 |
                40 |
                40 |
                10 |

taos> SELECT lag(val, 1) FROM t_dup;
 lag(val, 1) |
==============
 NULL        |
          10 |
          20 |

taos> SELECT lead(val, 1) FROM t_dup;
 lead(val, 1) |
===============
           20 |
           30 |
 NULL         |

taos> SELECT fill_forward(val) FROM t_fill_dup;
 fill_forward(val) |
====================
 NULL              |
                 0 |
                50 |

taos> SELECT count(*) FROM t_base INTERVAL(2s);
       count(*)        |
========================
                     1 |
                     2 |
                     2 |

taos> SELECT count(*) FROM t_base SESSION(ts, 1s);
       count(*)        |
========================
                     5 |

taos> SELECT count(*) FROM t_base STATE_WINDOW(st);
       count(*)        |
========================
                     1 |
                     2 |
                     1 |
                     1 |

taos> SELECT count(*) FROM t_base EVENT_WINDOW START WITH val >= 30 END WITH val >= 40;
       count(*)        |
========================
                     3 |
                     1 |

taos> SELECT count(*) FROM t_base COUNT_WINDOW(2);
       count(*)        |
========================
                     2 |
                     2 |
                     1 |

taos> SELECT count(*) FROM (SELECT * FROM t_base ORDER BY ts DESC) INTERVAL(2s);
       count(*)        |
========================
                     1 |
                     2 |
                     2 |

taos> SELECT count(*) FROM (SELECT * FROM t_base ORDER BY ts DESC) SESSION(ts, 1s);
       count(*)        |
========================
                     5 |

taos> SELECT count(*) FROM (SELECT * FROM t_base ORDER BY ts DESC) STATE_WINDOW(st);
       count(*)        |
========================
                     1 |
                     2 |
                     1 |
                     1 |

taos> SELECT count(*) FROM (SELECT * FROM t_base ORDER BY ts DESC) EVENT_WINDOW START WITH val >= 30 END WITH val >= 40;
       count(*)        |
========================
                     1 |
                     1 |

taos> SELECT count(*) FROM (SELECT * FROM t_base ORDER BY ts DESC) COUNT_WINDOW(2);
       count(*)        |
========================
                     2 |
                     2 |
                     1 |

taos> SELECT count(*) FROM t_dup INTERVAL(2s);
       count(*)        |
========================
                     2 |
                     3 |

taos> SELECT count(*) FROM t_dup SESSION(ts, 1s);
       count(*)        |
========================
                     5 |

taos> SELECT count(*) FROM t_dup STATE_WINDOW(st);

DB error: Duplicate timestamp not allowed in count/event/state window [0x8000073E] 
taos> SELECT count(*) FROM t_dup EVENT_WINDOW START WITH val >= 20 END WITH val >= 30;

DB error: Duplicate timestamp not allowed in count/event/state window [0x8000073E] 
taos> SELECT count(*) FROM t_dup COUNT_WINDOW(2);

DB error: Duplicate timestamp not allowed in count/event/state window [0x8000073E] 
taos> SELECT _wstart, _wend, count(*) FROM t_base external_window((SELECT ts, endtime FROM t_ext_bounds) w);
         _wstart         |          _wend          |       count(*)        |
============================================================================
 2024-01-01 00:00:01.000 | 2024-01-01 00:00:02.000 |                     2 |
 2024-01-01 00:00:03.000 | 2024-01-01 00:00:05.000 |                     3 |

taos> SELECT _wstart, _wend, count(*) FROM (SELECT * FROM t_base ORDER BY ts DESC) external_window((SELECT ts, endtime FROM t_ext_bounds) w);
         _wstart         |          _wend          |       count(*)        |
============================================================================
 2024-01-01 00:00:03.000 | 2024-01-01 00:00:05.000 |                     5 |

taos> SELECT _wstart, _wend, count(*) FROM t_dup external_window((SELECT ts, endtime FROM t_ext_bounds) w);
         _wstart         |          _wend          |       count(*)        |
============================================================================
 2024-01-01 00:00:01.000 | 2024-01-01 00:00:02.000 |                     4 |
 2024-01-01 00:00:03.000 | 2024-01-01 00:00:05.000 |                     1 |

taos> SELECT interp(val) FROM t_base RANGE('2024-01-01 00:00:01', '2024-01-01 00:00:05') EVERY(1s) FILL(LINEAR);
 interp(val) |
==============
          30 |
          10 |
          50 |
          20 |
          40 |

taos> SELECT interp(val) FROM (SELECT * FROM t_base ORDER BY ts DESC) RANGE('2024-01-01 00:00:01', '2024-01-01 00:00:05') EVERY(1s) FILL(LINEAR);
 interp(val) |
==============
          40 |

taos> SELECT interp(val) FROM t_dup RANGE('2024-01-01 00:00:01', '2024-01-01 00:00:03') EVERY(1s) FILL(LINEAR);
 interp(val) |
==============
          20 |
          30 |

