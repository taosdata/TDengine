# Auto-generated INFORMATION_SCHEMA whitelist
# from_version : 3.4.0.0
# to_version   : 3.4.0.8
# generated_at : 2026-03-16 11:17:23
#
# All changes listed here are treated as expected for this upgrade path.
# Rename/copy with a shorter version prefix to apply more broadly:
#   e.g.  3.3~3.4.yaml  applies to all 3.3.x.x -> 3.4.x.x upgrades

modified_tables:
- table: INS_DATABASES
  added_columns:
  - allow_drop
- table: INS_XNODE_AGENTS
  added_columns:
  - token
  - status
  deleted_columns:
  - scope
  changed_position:
  - col: create_time
    before: 3
    after: 4
  - col: update_time
    before: 4
    after: 5
- table: INS_XNODE_JOBS
  changed_type:
  - col: config
    before: VARCHAR(4096)
    after: VARCHAR(49152)
- table: INS_XNODE_TASKS
  added_columns:
  - created_by
  - labels
  changed_type:
  - col: from
    before: VARCHAR(1024)
    after: VARCHAR(4096)
  - col: to
    before: VARCHAR(1024)
    after: VARCHAR(2048)
  - col: parser
    before: VARCHAR(4096)
    after: VARCHAR(49152)
  changed_position:
  - col: create_time
    before: 9
    after: 11
  - col: update_time
    before: 10
    after: 12
