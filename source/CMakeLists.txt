add_subdirectory(os)
add_subdirectory(util)
add_subdirectory(common)
add_subdirectory(libs)
if (${BUILD_THIN_CLIENT})
    add_subdirectory(thinClient)
endif(${BUILD_THIN_CLIENT})
add_subdirectory(client)
add_subdirectory(dnode)