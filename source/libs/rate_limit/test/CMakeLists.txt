CMAKE_MINIMUM_REQUIRED(3.2)

SET(RATE_LIMIT_TEST_DIR ${CMAKE_CURRENT_SOURCE_DIR})

ADD_EXECUTABLE(rate_limit_test
    "${RATE_LIMIT_TEST_DIR}/rateLimitTest.c"
)

TARGET_INCLUDE_DIRECTORIES(rate_limit_test PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/../inc")
TARGET_LINK_LIBRARIES(rate_limit_test PRIVATE rate_limit)

IF(TD_WINDOWS)
    TARGET_LINK_LIBRARIES(rate_limit_test PRIVATE pthread)
ENDIF()
