use super::*;

#[tokio::test]
async fn test_lush_stream() -> anyhow::Result<()> {
    consume_lush_record(pool, taos, stmt, record, columns, names, marks, records, license, transferred)
    tokio::task::spawn(async move {
        let
    })
    Ok(())
}
