CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(taos-tools
        LANGUAGES C
        VERSION 0.1.0
        )

if(NOT WIN32)
    string(ASCII 27 Esc)
    set(ColourReset "${Esc}[m")
    set(ColourBold  "${Esc}[1m")
    set(Red         "${Esc}[31m")
    set(Green       "${Esc}[32m")
    set(Yellow      "${Esc}[33m")
    set(Blue        "${Esc}[34m")
    set(Magenta     "${Esc}[35m")
    set(Cyan        "${Esc}[36m")
    set(White       "${Esc}[37m")
    set(BoldRed     "${Esc}[1;31m")
    set(BoldGreen   "${Esc}[1;32m")
    set(BoldYellow  "${Esc}[1;33m")
    set(BoldBlue    "${Esc}[1;34m")
    set(BoldMagenta "${Esc}[1;35m")
    set(BoldCyan    "${Esc}[1;36m")
    set(BoldWhite   "${Esc}[1;37m")
endif()

IF (${CMAKE_SYSTEM_NAME} MATCHES "Linux" OR ${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
    find_package(PkgConfig REQUIRED)

    IF (${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
        SET(OS_INFO "Darwin")
    ELSE()
        # freemine: find_package(ZLIB)
        # freemine: if (ZLIB_FOUND)
        # freemine:     set(ZLIB_PKG zlib)
        # freemine:     add_definitions(-DDEFLATE_CODEC)
        # freemine:     include_directories(${ZLIB_INCLUDE_DIRS})
        # freemine:     message("${Green} Enabled deflate codec ${ColourReset}")
        # freemine: else (ZLIB_FOUND)
        # freemine:     set(ZLIB_PKG "")
        # freemine:     set(ZLIB_LIBRARIES "")
        # freemine:     message("${Red} Disabled deflate codec. zlib not found. ${ColourReset}")
        # freemine: endif (ZLIB_FOUND)

        # freemine: pkg_check_modules(SNAPPY snappy)
        # freemine: IF (SNAPPY_FOUND AND ZLIB_FOUND)  # Snappy borrows crc32 from zlib
        # freemine:     set(SNAPPY_PKG libsnappy)
        # freemine:     add_definitions(-DSNAPPY_CODEC)
        # freemine:     include_directories(${SNAPPY_INCLUDE_DIRS})
        # freemine:     message("${Green} Enabled snappy codec ${ColourReset}")
        # freemine: ELSE ()
        # freemine:     execute_process(COMMAND chmod 777 ${PROJECT_SOURCE_DIR}/packaging/tools/get_os.sh)

        # freemine:     EXECUTE_PROCESS(COMMAND readlink /bin/sh OUTPUT_VARIABLE SHELL_LINK)
        # freemine:     MESSAGE(STATUS "The shell is: " ${SHELL_LINK})
        # freemine:     IF (${SHELL_LINK} MATCHES "dash")
        # freemine:         EXECUTE_PROCESS(COMMAND ${PROJECT_SOURCE_DIR}/packaging/tools/get_os.sh "" OUTPUT_VARIABLE OS_INFO)
        # freemine:     ELSE ()
        # freemine:         EXECUTE_PROCESS(COMMAND sh ${PROJECT_SOURCE_DIR}/packaging/tools/get_os.sh "" OUTPUT_VARIABLE OS_INFO)
        # freemine:     ENDIF()
        # freemine:     MESSAGE("Current OS is " ${OS_INFO})
        # freemine:     set(SNAPPY_PKG "")
        # freemine:     set(SNAPPY_LIBRARIES "")
        # freemine:     MESSAGE("")
        # freemine:     IF (${OS_INFO} MATCHES "CentOS")
        # freemine:         MESSAGE("${Yellow}libsnappy not found or zlib not found.")
        # freemine:         MESSAGE("NOTE: The snappy package lacks pkg-config support on CentOS Linux even it is already installed.")
        # freemine:         MESSAGE("      It leads cmake prompt \"libsnappy not found.\", but it will works well.")
        # freemine:         MESSAGE("      Please refer to https://github.com/google/snappy/pull/86.")
        # freemine:         MESSAGE("${ColourReset}")
        # freemine:     ELSE ()
        # freemine:         MESSAGE("${Red}libsnappy not found or zlib not found.")
        # freemine:         MESSAGE("${ColourReset}")
        # freemine:     ENDIF ()
        # freemine: ENDIf (SNAPPY_FOUND AND ZLIB_FOUND)

        # freemine: find_package(PkgConfig)
        # freemine: pkg_check_modules(LZMA liblzma)
        # freemine: if (LZMA_FOUND)
        # freemine:     set(LZMA_PKG liblzma)
        # freemine:     add_definitions(-DLZMA_CODEC)
        # freemine:     include_directories(${LZMA_INCLUDE_DIRS})
        # freemine:     link_directories(${LZMA_LIBRARY_DIRS})
        # freemine:     message("${Green} Enabled lzma codec ${ColourReset}")
        # freemine: else (LZMA_FOUND)
        # freemine:     set(LZMA_PKG "")
        # freemine:     set(LZMA_LIBRARIES "")
        # freemine:     message("${Red} Disabled lzma codec. liblzma not found.${ColourReset}")
        # freemine: endif (LZMA_FOUND)

        # freemine: # Jansson JSON library
        # freemine: pkg_check_modules(JANSSON jansson>=2.3)
        # freemine: IF (JANSSON_FOUND)
        # freemine:     MESSAGE("${Green} jansson found ${ColourReset}")
        # freemine:     include_directories(${JANSSON_INCLUDE_DIRS})
        # freemine: ELSE()
        # freemine:     MESSAGE(FATAL_ERROR "${Red} jansson not found ${ColourReset}")
        # freemine: ENDIF()
    ENDIF ()
ENDIF ()

SET(EXECUTABLE_OUTPUT_PATH ${CMAKE_BINARY_DIR}/build/bin)

IF (${VERBOSE} MATCHES "true")
    SET(CMAKE_VERBOSE_MAKEFILE ON)
ENDIF ()

ADD_SUBDIRECTORY(deps)
ADD_SUBDIRECTORY(src)

ADD_DEPENDENCIES(taosBenchmark toolscJson)

IF (${CMAKE_SYSTEM_NAME} MATCHES "Linux" OR ${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
    SET(MAKE_INSTALL_SH "${PROJECT_SOURCE_DIR}/packaging/tools/make_install.sh")
    INSTALL(CODE "execute_process(COMMAND bash ${MAKE_INSTALL_SH} ${CMAKE_BINARY_DIR})")
ELSEIF (${CMAKE_SYSTEM_NAME} MATCHES "Windows")
    SET(MAKE_INSTALL_SH "${PROJECT_SOURCE_DIR}/packaging/tools/make_install.bat")
    INSTALL(CODE "execute_process(COMMAND ${MAKE_INSTALL_SH} :needAdmin ${PROJECT_SOURCE_DIR} ${PROJECT_BINARY_DIR} Windows)")
ENDIF ()

# add test
IF(TD_LINUX)
    # unit test
    IF(${BUILD_TEST})
        ADD_SUBDIRECTORY(test)
    ENDIF(${BUILD_TEST})    
ENDIF()
