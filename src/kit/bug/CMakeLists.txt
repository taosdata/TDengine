CMAKE_MINIMUM_REQUIRED(VERSION 3.5)
PROJECT(TDengine)

INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/src/client/inc)
INCLUDE_DIRECTORIES(inc)

IF (TD_LINUX)
  AUX_SOURCE_DIRECTORY(. SRC)
  ADD_EXECUTABLE(bug ${SRC})

  IF (TD_SOMODE_STATIC)
    TARGET_LINK_LIBRARIES(bug taos_static)
  ELSE ()
    TARGET_LINK_LIBRARIES(bug taos)
  ENDIF ()
ELSEIF (TD_WINDOWS)
  AUX_SOURCE_DIRECTORY(. SRC)
  ADD_EXECUTABLE(bug ${SRC})
  TARGET_LINK_LIBRARIES(bug taos_static)
ELSEIF (TD_DARWIN)
  AUX_SOURCE_DIRECTORY(. SRC)
  ADD_EXECUTABLE(bug ${SRC})

  IF (TD_SOMODE_STATIC)
    TARGET_LINK_LIBRARIES(bug taos_static)
  ELSE ()
    TARGET_LINK_LIBRARIES(bug taos)
  ENDIF ()
ENDIF ()
