# taos-connector-go is a pure Go library consumed via `go get`.
# It does not produce compiled artifacts (no .so/.a/binary).
# Source code is packaged as-is; no build step is needed.
message(STATUS "")
message(STATUS "=== taos-connector-go ===")
message(STATUS "  TD_CONNECTOR_GO_DIR = ${TD_CONNECTOR_GO_DIR}")
message(STATUS "  No compilation required — source will be packaged as-is.")
message(STATUS "=========================")
message(STATUS "")
