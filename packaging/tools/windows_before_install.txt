TDengine CLI will be installed in the directory selected in this wizard.

After installation, open a terminal and run taos to connect using the default settings.
Run taos --help to view connection and authentication options.
