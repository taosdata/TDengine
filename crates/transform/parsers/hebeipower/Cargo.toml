[package]
name = "hebeipower"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
regex = "1.4"
tracing = "0.1"
mimalloc = { version = "0.1", optional = true }
chrono = { version = "0.4.37" }

[features]
default = ["mimalloc"]
