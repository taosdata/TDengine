# ODBC 连接器-Requirement Spec

## 1. 修订记录

| 编写日期 | 发布日期 | 版本 | 修订人 | 主要修改内容 |
| --- | --- | --- | --- | --- |
| 2025-01-14 | 2025-01-14 | 1.0 | 裴亚明 | 创建文档 |
| 2025-12-14 | 2025-12-14 | 1.1 | 裴亚明 | 修改完善文档内容 |
| 2025-12-19 | 2025-12-19 | 1.2 | 霍琳贺 | 添加安全需求 |

## 2. 引言

在当今物联网(IoT)与工业互联网蓬勃发展的时代，时序数据的高效存储和实时分析对于众多行业来说至关重要。TDengine 作为一款为物联网环境量身定制的开源时序数据库，以其高吞吐量、高压缩比以及强大的查询性能而著称，适用于各种需要处理大规模时间序列数据的应用场景。
随着企业级应用开发环境的不断演进，特别是那些依赖于开放数据库连接(ODBC)标准接口的系统，对于 TDengine 的集成需求日益增长。ODBC 是一种允许最大范围编程语言和操作系统访问不同数据库管理系统的应用程序编程接口(API)，它不仅为 TDengine 提供了一个桥梁，能够无缝对接各类应用程序，还极大地拓宽了 TDengine 在不同技术栈中的应用场景。
为了使 TDengine 更好地融入这些广泛的企业级应用开发环境中，设计并实现一个兼容 ODBC 标准的连接器变得尤为关键。本项目旨在开发出一个符合 ODBC 标准的 TDengine 连接器，以增强 TDengine 在企业级应用中的适用性，促进时序数据分析技术的发展，并为用户提供更加灵活多样的数据访问方式。

### 2.1 术语与缩写名词

1. **ODBC (Open Database Connectivity)：** 是一种开放式的标准应用程序编程接口（API），它为程序、脚本和商务智能工具提供了一种访问各种数据库管理系统（DBMS）的方法。ODBC 的设计是为了让 SQL 语句可以通过几乎任何编程语言在多个平台上执行，从而使得开发者可以编写不依赖于特定数据库的应用程序。
2. **DSN (Data Source Name)：**数据源名称是一个标识符，用来指代一个特定的数据库配置，其中包括了连接到该数据库所需的全部信息，例如服务器地址、数据库名称、认证凭据等。DSN 可以是系统 DSN（供所有用户使用）或用户 DSN（仅限当前用户），或者是文件 DSN（保存在文件中）。
3. **参数绑定(Parameter Binding)：**在 SQL 语句中使用占位符替代具体值的技术，以防止 SQL 注入并提升查询性能。通过将参数值与 SQL 代码分离，确保安全性，并允许数据库预编译语句，减少执行时间。
4. **Native：**指的是直接利用TDengine提供的C语言客户端库进行数据库交互。这种方式提供最底层、最高效的API调用，支持所有TDengine特性，适合追求性能和灵活性的应用开发，允许开发者直接在应用中嵌入TDengine功能，实现数据的快速读写与处理。
5. **WebSocket：**是一种基于 TCP 的全双工通信协议，支持服务器与客户端之间实时、双向的数据传输。它提供了一个持久连接，使得数据可以即时推送，而无需像 HTTP 那样每次交互都建立新连接。
6. **VQT：**指的是变量值（Value）、质量戳（Quality Timestamp）、时间戳（Time Timestamp） 的组合。这种形式常用于工业自动化、过程控制、数据采集系统和监控系统中，以确保记录的数据不仅包含数值本身，还包括其质量和采集时间的信息。

### 2.2 相关文档资料

Microsoft Open Database Connectivity (ODBC)：[https://learn.microsoft.com/en-us/sql/odbc/microsoft-open-database-connectivity-odbc](https://learn.microsoft.com/en-us/sql/odbc/microsoft-open-database-connectivity-odbc)

### 2.3 优先级要求

1. 重要程度
本需求至关重要，旨在开发符合 ODBC 标准的 TDengine 连接器，以提升其在企业级应用中的兼容性和适用性，扩大TDengine的应用范围并满足实时数据分析需求。这对TDengine的产品竞争力和市场拓展具有战略意义。
1. 期望交付时间
期望在 2024年3月中旬 完成初步版本开发，并于 2024年5月底前正式发布稳定版，确保及时响应市场需求。请优先资源分配和技术支持，确保按时高质量完成项目。

### 2.4 版本要求

支持 ODBC 3.0 标准
支持TDengine 3.3.0.0 及以上版本

## 3. 需求目标

本项目的主要目标是开发一个符合 ODBC 标准的 TDengine 连接器，以达成以下具体目标：
1. 提供标准接口:
   - 实现严格遵守 ODBC 规范的标准接口，支持常见的数据库操作，如连接、查询、插入等。
   - 确保 TDengine 可以通过 ODBC 接口被多种编程语言访问，从而提高其在不同技术栈中的适用性。
2. 支持核心特性:
   - 支持 TDengine 的关键功能，例如高效的 SQL 执行、参数绑定、批量插入等，确保用户能够充分利用数据库的性能优势。
3. 易于使用:
   - 构建一个易于集成和使用的 ODBC 驱动程序，附带详尽的技术文档和代码示例，帮助开发者快速上手。
4. 稳定性和可靠性:
   - 经过严格的测试流程，保证连接器的稳定性和可靠性，确保其能够在实际生产环境中成功部署和运行。

## 4. 功能需求

| 序号 | **功能类别** | **功能名称** | 功能描述 |
| --- | --- | --- | --- |
| 1 | ConfigDSN | 配置数据源（Data Source Name, DSN）以供后续连接使用。 |
| 2 | ConfigDriver | 配置ODBC驱动程序的属性和参数。 |
| 3 | ConfigTranslator | 配置ODBC翻译器，用于SQL语句的转换。 |
| 4 | SQLConnect | 使用已定义的数据源名称建立到数据库的连接。 |
| 5 | SQLDriverConnect | 直接通过连接字符串建立到数据库的连接，支持更多的连接选项。 |
| 6 | SQLDisconnect | 断开与数据库的现有连接。 |
| 7 | SQLSetConnectAttr | 设置连接属性，如超时时间等。 |
| 8 | SQLGetConnectAttr | 获取当前连接的属性值。 |
| 9 | SQLGetEnvAttr | 获取环境属性的当前设置。 |
| 10 | SQLSetEnvAttr | 设置环境属性，如ODBC版本等。 |
| 11 | SQLGetStmtAttr | 获取语句属性的当前设置。 |
| 12 | SQLSetStmtAttr | 设置语句属性，如游标类型等。 |
| 13 | SQLAllocHandle | 分配一个环境、连接或语句句柄。 |
| 14 | SQLFreeHandle | 释放指定类型的句柄，回收资源。 |
| 15 | SQLFreeStmt | 释放语句句柄并清理相关资源。 |
| 16 | SQLNumResultCols | 返回结果集中列的数量。 |
| 17 | SQLColumns | 检索指定表中的所有列信息。 |
| 18 | SQLTables | 检索数据库中所有表的信息。 |
| 19 | SQLPrimaryKeys | 检索指定表的主键信息。 |
| 20 | SQLFetch | 获取下一行数据。 |
| 21 | SQLFetchScroll | 按照指定的方式滚动获取多行数据。 |
| 22 | SQLGetData | 获取结果集中某列的值，适用于大对象或未绑定列。 |
| 23 | SQLDescribeCol | 描述结果集中某一列的信息。 |
| 24 | SQLColAttribute | 获取结果集中某一列的具体属性。 |
| 25 | SQLGetInfo | 获取有关数据源的各种信息。 |
| 26 | SQLGetTypeInfo | 检索数据源支持的数据类型信息。 |
| 27 | SQLBindParameter | 绑定参数到预处理语句中，支持输入和输出参数。 |
| 28 | SQLDescribeParam | 描述预处理语句中的参数信息。 |
| 29 | SQLNumParams | 获取预处理语句中参数的数量。 |
| 30 | SQLBindCol | 将结果集中的列绑定到应用程序变量。 |
| 31 | SQLMoreResults | 移动到下一个结果集，如果有的话。 |
| 32 | SQLRowCount | 获取最近执行的操作影响的行数。 |
| 33 | SQLCloseCursor | 关闭当前打开的游标。 |
| 34 | SQLPrepare | 准备一条SQL语句以便后续执行。 |
| 35 | SQLExecDirect | 直接执行一条SQL语句，无需预处理。 |
| 36 | SQLExecute | 执行之前准备好的SQL语句。 |
| 37 | SQLEndTran | 提交或回滚事务。 |
| 38 | SQLGetDiagField | 获取错误或状态记录中的特定字段。 |
| 39 | SQLGetDiagRec | 获取最新的错误或状态记录。 |

## 5. 安全需求

| 序号 | **功能类别** | **功能名称** | 功能描述 |
| --- | --- | --- | --- |
| 1 | 禁止明文存储凭证 | 不明文存储密码 |
| 2 | 连接字符串安全处理 | 驱动程序必须在内存中及时清除包含密码的连接字符串，不得将其写入日志文件。 |
| 3 | 支持 TLS/SSL 加密 | 在 DSN 配置中提供启用加密的选项，并在文档中明确说明如何配置。对于生产环境，建议默认启用加密。 |
| 4 | 数据完整性校验 | 使用带有完整性校验的加密协议或自定义协议。 |
| 5 | 可配置的超时机制 | 支持查询超时。 |
| 6 | 错误消息安全处理 | 错误消息不得包含敏感信息如完整文件路径、数据库内部结构、SQL 语句细节等。 |
| 7 | 可配置的审计日志 | 记录关键操作包括：连接建立/断开（包括源 IP、用户名、时间）、SQL 语句执行（语句类型、执行时间、影响行数）、认证失败、错误和异常。日志应包含足够信息用于安全分析和事件追溯。 |

## 6. 性能需求

| 使用场景 | 类别 | 要求 |
| --- | --- | --- |
| 支持10个子表并发查询，每个查询5000条记录 | 查询 | 2秒内完成查询 |
| 支持10000个子表同时查询最新数据 | 查询 | 1秒内完成查询 |
| 支持10000个子表同时写入最新数据 | 写入 | 1秒内完成写入 |
| 支持写入事件记录数据，30个字段左右 | 写入 | 每秒数据写入1000条 |
| 支持20个客户端并发执行，查询10000个子表的最新数据和写入10000个子表的最新数据 | 稳定性 | 持续3*24小时压测，无异常 |

在工业互联网中，建立的模型是通常是VQT形式，就是变量值，质量戳、时间戳，标签可以设置为：factory、area、equipment、tagName、datasource、unit 等6-10个字段。

## 7. 其他需求

描述功能、性能之外的需求。
1. 兼容性需求
   - 兼容 TDengine 3.3.0.0 及更高版本。
2. 接口需求
   - 支持ODBC 3.0标准。
3. 运维需求
4. 易用性需求
   - 接口设计合理，调用简单。
5. 测试需求
   - 对主要功能以及异常情况都需要有测试用例覆盖
