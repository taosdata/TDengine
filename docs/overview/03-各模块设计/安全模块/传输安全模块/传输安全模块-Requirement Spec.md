# 传输安全模块-Requirement Spec

## 1. 修订记录

| 编写日期 | 发布日期 | 版本 | 修订人 | 主要修改内容 |
| --- | --- | --- | --- | --- |
| 2025-09-11 | 2025-10-14 | 1.0 | 关胜亮 | 新建 |
| 2025-12-09 | 2025-12-11 | 1.1 | 程洪泽 | 修订及完善内容 |

## 2. 引言

### 2.1 术语与缩写名词

| **术语/缩写** | **全称/解释** |
| --- | --- |
| **SQL 注入** | 一种通过在 SQL 语句中插入恶意代码，从而欺骗数据库执行非预期操作的攻击方式 |
| **TLS** | 传输层安全协议（Transport Layer Security） |
| **SASL** | 简单认证和安全层（Simple Authentication and Security Layer） |
| **RPC** | 远程过程调用（Remote Procedure Call） |
| **SALT** | 随机盐值，用于密码学操作增加随机性 |
| **CFG_KEY** | 配置密钥，用于加密配置文件 |
| **SYSSSO** | 系统超级安全管理员（System Super Security Officer） |
| **SSF** | 安全强度因子（Security Strength Factor） |

### 2.2 相关文档资料

JIRA [TS-7229](https://jira.taosdata.com:18080/browse/TS-7229?src=confmacro)

### 2.3 优先级要求

高

### 2.4 版本要求

1. 企业版支持
2. 社区版不支持

## 3. 需求目标

对重要数据的传输过程进行加密。
1. 防 SQL 注入
2. 防溢出攻击
3. 白名单设计过于简单，需增强功能
4. TLS 外可能还需其他机制
5. 数据包大小无限制
6. 传输层做消息校验，防止恶意消息耗尽内存
7. 防范消息重放攻击
8. 连接管理，登录用户 session 管理
9. 多副本状态切换的消息认证机制
10. 集群状态变换的消息认证机制

## 4. 功能需求

### 4.1 防 SQL 注入

1. **黑名单机制**：维护一个已知攻击特征库（如常见的`OR 1=1`、`UNION SELECT`等模式），直接拦截匹配的恶意请求 。
2. **白名单机制**（更为安全）：防火墙可以工作在“学习模式”下，记录下一段时间内应用系统正常产生的所有SQL语句，自动生成一个“合法SQL语句集合”（即白名单）

### 4.2 防溢出攻击

1. 防止攻击者执行恶意代码或获取控制权
2. 利用程序漏洞，向内存缓冲区填入**超长数据**，覆盖并篡改相邻的关键数据，从而改变程序执行流程

### 4.3 TLS 安全性增强

在 3.3.8 版本已经支持基于 TLS 的证书传输，但其安全性还需要进一步增强，涉及的点有
1. 私钥文件安全存放：使用 “存储安全” 需求中提到的 CFG_KEY 对证书相关的密钥文件进行加密，加密后存储到 dnode/config 文件夹下。加密触发由 taosk 手动进行，记录加密时间。
2. 私钥文件更换时间查看：与[ 存储安全需求文档](https://taosdata.feishu.cn/wiki/UYAqwU3GqiBsjCkT6BccKqLmnGh) 中的 “密钥变更” 节中 “查看SVR_KEY、DB_KEY” 的 SQL 语句一并设计
3. 私钥文件定期更换：与[ 存储安全需求文档](https://taosdata.feishu.cn/wiki/UYAqwU3GqiBsjCkT6BccKqLmnGh) 中的 “密钥变更” 节中 “设置密钥到期时间和到期策略” 的 SQL 语句一并设计

### 4.4 通信安全性增强

当开启 TLS 传输时，所有内部通信，包括多副本状态切换、集群状态变换等消息，均通过 TLS 加密。此外，在传输层，增强如下校验。
1. 为消息增加随机的 SALT 、序列号或者时间戳，避免消息重放攻击
2. 对数据包进行安全性校验，防止恶意消息耗尽内存，即改造现有的消息队列机制
3. 增强数据包的完整性校验，防止消息在传输中被篡改，现有 checksum 机制安全性不够
4. 对数据包大小进行限制，防止恶意传输大数据包

### 4.5 关键信息传输

开发一套 SASL 机制，在 TLS 传输基础上，增强关键信息的传输安全。
1. 在 RPC 层提供单独的传输接口，在业务层可以透明调用
2. 修改“创建用户、修改用户、用户登录、秘钥更改、关键参数”等关键信息的发送过程，使用 SASL 通信
SASL 采用挑战-响应机制，可集成 `libsasl` 库中的 API 来实现。参照的 API 过程如下：

| **阶段** | **主要任务** | **关键 SASL API (示例)** |
| --- | --- | --- |
| **初始化** | 加载库、插件和机制；建立连接上下文；设置安全属性和回调函数。 | sasl_client_init(), sasl_server_init() |
| **机制协商** | 服务端通告支持的机制；客户端选择最合适的机制（通常基于安全强度）。 | sasl_listmech(), sasl_client_start() |
| **认证交互** | 服务端发出挑战（Challenge）；客户端计算并返回响应（Response）；可能多轮往返。 | sasl_server_step(), sasl_client_step() |
| **安全层协商** | 认证成功后，协商后续通信是否启用以及使用何种安全层（完整性、保密性）。 | 自动完成，通过 sasl_getprop()查询 SSF |
| **安全通信** | 若协商了安全层，后续应用数据需通过 sasl_encode()和 sasl_decode()进行加密解密。 | sasl_encode(), sasl_decode() |
| **连接释放** | 认证失败或会话结束后，释放连接上下文和相关资源。 | sasl_dispose(), sasl_done() |

### 4.6 白名单增强

1. 在 [身份鉴别需求文档](https://taosdata.feishu.cn/wiki/GZNPwH62SiiRtQkQHTvcM73YnDh) 中的创建用户语法定义中，增加了 <禁止HOST> 子句，需在白名单中处理此逻辑。
2. 还需和 taosadapter 联调，处理禁止 host 的相关逻辑，因此这个功能还需提早开发，避免阻塞 taosadapter 的开发任务。 

### 4.7 连接管理

在 [身份鉴别需求文档](https://taosdata.feishu.cn/wiki/GZNPwH62SiiRtQkQHTvcM73YnDh) 中的创建用户语法定义中，增加了如下选项
1. SESSION_PER_USER 
2. CONNECT_IDLE_TIME 
3. CONNECT_TIME 
4. CALL_PER_SESSION 
5. VNODE_PER_CALL 
6. <允许时间子句> 
7. <禁止时间子句>
请处理这些选项的逻辑。此外，还需在系统表中，对 connections 记录更多必要的连接信息，例如
1. connect_idle_time
2. max_call_per_session
3. max_call_per_vnode
4. username
5. token_id

### 4.8 检测暴力破解

1. 详细记录各类通信失败，尤其是登录失败，包括：时间戳、尝试使用的用户名、来源IP地址、失败原因
2. 通信失败应实时写入监控库
3. 出现严重的异常行为时，例如当某用户名或IP的失败次数在设定的时间窗口（如 1 分钟、5 分钟）内超过阈值（如 5 次），即可判定为异常行为。
   - 进行告警（写入监控库、配置告警策略）
   - 暂时锁定该用户账号或来源 IP

## 5. 性能需求

传输安全在保证通信安全性的同时，也会带来一定的性能影响。在测试过程中，对如下指标进行测试，如果性能不达预期，需优化代码。
1. **写入性能**：
   - 加密实现影响数据写入性能，写入延迟增加不得超过 100%。
2. **查询性能**：
   - 加密实现影响数据查询性能，查询延迟增加不得超过 100%。

## 6. 安全可控需求

### 6.1 安全需求

#### 6.1.1 权限控制

1. **最小权限原则**：所有安全功能遵循最小权限原则
2. **SYSSSO权限**：连接管理、白名单查看等敏感操作仅SYSSSO角色具备权限
3. **操作审计**：所有安全相关操作应有完整的审计日志
4. **权限分离**：安全管理员与系统管理员权限分离

#### 6.1.2 密钥安全

1. **密钥存储**：所有密钥（TLS私钥、CFG_KEY等）必须加密存储
2. **密钥访问**：密钥访问需严格的权限控制和审计
3. **密钥轮换**：支持定期密钥轮换机制
4. **密钥销毁**：支持安全的密钥销毁机制

#### 6.1.3 代码安全

1. **安全编码**：遵循安全编码规范，避免常见安全漏洞
2. **代码审计**：安全关键代码需经过专门的安全审计
3. **依赖管理**：第三方安全库需进行安全评估和版本管理
4. **漏洞管理**：建立安全漏洞响应和修复机制

### 6.2 可控性需求

#### 6.2.1 自主可控

1. **核心算法**：核心安全算法优先考虑自主实现
2. **代码透明**：安全模块代码保持透明度，便于审查
3. **技术栈**：优先选择自主可控的技术栈和工具链

#### 6.2.2 第三方库管理

1. **审慎引入**：如必须引入第三方库，需进行严格安全评估
2. **许可证合规**：确保所有第三方库许可证符合项目要求
3. **版本锁定**：对第三方库版本进行锁定和管理
4. **应急替换**：对关键第三方库制定应急替换方案

#### 6.2.3 供应链安全

1. **来源可信**：确保所有软件组件来源可信
2. **完整性校验**：对下载的软件组件进行完整性校验
3. **漏洞监控**：监控第三方组件的安全漏洞信息
4. **及时更新**：对发现的安全漏洞及时更新修复

## 7. 兼容性需求

### 7.1 向后兼容

1. **版本升级**：自3.3.8版本升级至本版本时，不需要手工干预
2. **数据兼容**：加密数据、配置信息等保持向前兼容
3. **接口兼容**：对外接口保持向后兼容，避免影响现有应用

### 7.2 语法兼容

1. **无新增语法**：当前需求不新增SQL语法，不影响现有SQL语句
2. **参数扩展**：通过扩展现有语法参数实现新功能
3. **默认行为**：新增功能默认关闭或使用安全默认值
