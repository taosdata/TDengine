# 系统表信息补充

如下的几张系统表在官网文档还没有公开，但在taos shell中已经能够看到了，需要尽快进行内部测试，因此需要补充描述信息。

## 1. ins_modules 

@陈东明 请提供一下详细信息供测试组验证，此系统表不公开
taosd里未实现ins_modules的查询功能

## 2. ins_snodes

@廖浩均 请提供一下详细信息供测试组验证，此系统表不公开

| id | int | Snode 的 id |
| --- | --- | --- |
| endpoint | varchar | Snode 的 ep |
| create_time | timestamp | 节点创建时间 |


## 3. ins_stream_tasks

@廖浩均 同上

| stream_name | varchar | 流名称 |
| --- | --- | --- |
| task_id | int | 流启动任务的id |
| node_type | varchar | 任务运行的节点类型（vnode/snode) |
| node_id | int | 运行node 的 id，如果是 vnode，就是 vgroup id，如果是 snode，就是 snode 的id |
| level | varchar | Task 的级别（source/agg/sink） |
| status | varchar | 任务执行状态（现在只会显示 normal） |


## 4. ins_vnodes

@陈东明 请提供一下详细信息供测试组验证，此系统表不公开
所有vnodes的列表，字段包括如下：
vgroup_id：所属的vgroupid
replica：所属vgroup的副本数量
status：该vnode的当前状态（leader, follower, offline）
db_name：所属db
dnode_id：所在dnode的id
dnode_ep：所在dnode的ep

## 5. ins_user_privileges

@潘魏 请提供一下详细信息供测试组验证，此系统表不公开。用户查询权限信息仍然通过 show grants
