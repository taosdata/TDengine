# 流计算功能测试用例

复合主键对三种 trigger_mode 无任何影响，故仅对 at_once 模式用例细化设计，window_close 和 max_delay 模式挑选基础场景进行验证；
**覆盖简图：**

|  |
| --- |
|  | interval | count | state | session | scalar_function | interval | count | state | session | interval | count | state | session |
| No partition | ✅ | ✅ |  |  | ✅ |  |  |  |  |  |  |  |  |
| Partition by tbname | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |  |  |  | ✅ |  |  |  |
| Partition by column | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| Partition by tag | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| Partition by expression | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| delete | ✅ | ✅ | ✅ | ✅ |  | ✅ |  |  |  | ✅ |  |  |  |
| update | ✅ | ✅ | ✅ | ✅ |  | ✅ |  |  |  | ✅ |  |  |  |
| disorder | ✅ | ✅ | ✅ | ✅ |  | ✅ |  |  |  | ✅ |  |  |  |
| ignore_expired | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| ignore_update | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| existed_stable | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| custom_tag | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| fill_history | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |  |  |  | ✅ |  |  |  |
| watermark |  | ✅ |  |  |  | ✅ |  |  |  | ✅ |  |  |  |
| snode | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |  |  |  | ✅ |  |  |  |
| checkpoint | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| subtable | ✅ | ✅ | ✅ | ✅ |  | ✅ |  |  |  | ✅ |  |  |  |
| pause | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| resume | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |
| fill | ✅ |  |  |  |  |  |  |  |  |  |  |  |  |



| 测试类型 | 测试场景 | 用例No. | 测试用例名称 | 测试步骤 | 测试结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1、【基础用例】 | Interval + 无 partition | 1. 建流语句使用 interval，无 partition； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 2、【基础用例】 | Interval + partition by tbname | 1. 建流语句使用 interval + partition by tbname； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 3 | Interval + partition by column | 1. 建流语句使用 interval + partition by column； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 4 | Interval + partition by tag | 1. 建流语句使用 interval + partition by column； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 5 | Interval + partition by expression | 1. 建流语句使用 interval + partition by column； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 6、【基础用例】 | Interval + ignore_expired + disorder | 1. 建流语句分别设置 ignore_expired=0/1； 1. 写入 N 批数据并对比流批结果； 1. 写入乱序数据到未关闭窗口； 1. 对比流批结果； 1. 写入乱序数据到已关闭窗口； 1. 对比流批结果； | 4.流批结果相同 6.流批结果在 ignore_expired=0 时相同，ignore_expired=1 时不同 | pass |
| 7、【基础用例】 | Interval + ignore_expired + delete | 1. 建流语句分别设置 ignore_expired=0/1； 1. 写入 N 批数据并对比流批结果； 1. 删除未关闭窗口的某条数据； 1. 对比流批结果； 1. 删除已关闭窗口的某条数据； 1. 对比流批结果； | 4.流批结果相同 6.流批结果在 ignore_expired=0 时相同，ignore_expired=1 时不同 | pass |
| 8、【基础用例】 | Interval + ignore_update + update | 1. 建流语句分别设置 ignore_update=0/1； 1. 写入 N 批数据并对比流批结果； 1. 更新未关闭窗口的某条数据； 1. 对比流批结果； 1. 更新已关闭窗口的某条数据； 1. 对比流批结果； | 4.流批结果相同 6.流批结果在 ignore_update=0 时相同，ignore_update=1 时不同 | pass |
| 9 | Interval + existed_stb + custom_tag | 1. 建流语句分别使用已存在超级表和自定义 tag； 1. 写入 N 批数据并对比流批结果； | cast 后的流批结果相同 | pass |
| 10、【基础用例】 | Interval + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=0/1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | 流批结果在 fill_history=0 时相同，fill_history=1 时不同 | pass |
| 11、【基础用例】 | Interval + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | snode 覆盖所有用例，不影响原逻辑 | pass |
| 12、【基础用例】 | Interval + checkpoint | 1. 设置 checkpointInterval = 60 并启动 taosd 1. 建流； 1. 写入几条数据等待 60 s以上； 1. 写入 N 批数据并对比流批结果； | Checkpoint 后不影响原用例结果 | pass |
| 13 | Interval + subtable | 1. 建流语句使用 subtable； 1. 写入数据并确认 subtable 是否成功创建并写入数据； | subtable 成功创建并写入数据 | pass |
| 14 | Interval + pause/resume | 1. 建流语句使用 interval； 1. 写入数据过程中多次 pause/resume stream 并对比流批结果； | pause 后写入的数据不会计算在流结果中，resume 后可恢复计算，且流批结果相同 | pass |
| 15 | Interval + fill | 1. 建流语句分别使用 ["NULL", "PREV", "NEXT", "LINEAR", "VALUE"]； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 16 | count_window + 无 partition | 1. 建流语句使用 count_window，无 partition； 1. 写入 N 批数据并对比流批结果； | pass |
| 17 | count_window + partition by tbname | 1. 建流语句使用 count_window + partition by tbname； 1. 写入 N 批数据并对比流批结果； | pass |
| 18 | count_window + disorder | 1. 建流语句使用 count_window + partition by tbname； 1. 写入 N 批数据； 1. 写入乱序数据； 1. 对比流批结果； | pass |
| 19 | count_window + delete | 1. 建流语句使用 count_window + partition by tbname； 1. 写入 N 批数据； 1. 更新某条数据； 1. 对比流批结果； | pass |
| 20 | count_window + update | 1. 建流语句使用 count_window + partition by tbname； 1. 写入 N 批数据； 1. 删除某条数据； 1. 对比流批结果； | pass |
| 21 | count_window + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | pass |
| 22 | count_window + watermark | 1. 建流语句指定 watermark=***； 1. 写入 N 批数据并对比流批结果； | pass |
| 23 | count_window + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | pass |
| 24 | count_window + subtable | 1. 建流语句使用 subtable； 1. 写入数据并确认 subtable 是否成功创建并写入数据； | pass |
| 25 | state_window + partition by tbname | 1. 建流语句使用 state_window + partition by tbname； 1. 写入 N 批数据并对比流批结果； | pass |
| 26 | state_window + disorder | 1. 建流语句使用 state_window + partition by tbname； 1. 写入 N 批数据； 1. 写入乱序数据； 1. 对比流批结果； | pass |
| 27 | state_window + update | 1. 建流语句使用 state_window + partition by tbname； 1. 写入 N 批数据； 1. 更新某条数据； 1. 对比流批结果； | pass |
| 28 | state_window + delete | 1. 建流语句使用 state_window + partition by tbname； 1. 写入 N 批数据； 1. 删除某条数据； 1. 对比流批结果； | pass |
| 29 | state_window + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | pass |
| 30 | state_window + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | pass |
| 31 | state_window + subtable | 1. 建流语句使用 subtable； 1. 写入数据并确认 subtable 是否成功创建并写入数据； | pass |
| 32 | session + partition by tbname | 1. 建流语句使用 session + partition by tbname； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 33 | session_window + disorder | 1. 建流语句使用 session_window ignore_expired=0 + partition by tbname； 1. 写入 N 批数据； 1. 写入乱序数据； 1. 对比流批结果； | 流批结果相同 | pass |
| 34 | session_window + update | 1. 建流语句使用 session_window + ignore_update=0 + partition by tbname； 1. 写入 N 批数据； 1. 更新某条数据； 1. 对比流批结果； | 流批结果相同 | pass |
| 35 | session_window + delete | 1. 建流语句使用 session_window + partition by tbname； 1. 写入 N 批数据； 1. 删除某条数据； 1. 对比流批结果； | 流批结果相同 | pass |
| 36 | session_window + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | 流批结果相同 | pass |
| 37 | session_window + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | snode 覆盖所有用例，不影响原逻辑 | pass |
| 38 | session_window + subtable | 1. 建流语句使用 subtable； 1. 写入数据并确认 subtable 是否成功创建并写入数据； | subtable 成功创建并写入数据 | pass |
| 39、【基础用例】 | scalar_function + partition by tbname | 1. 建流语句使用 scalar_function + partition by tbname； 1. 写入 N 批数据并对比流批结果； | 流批结果相同 | pass |
| 40、【基础用例】 | scalar_function + disorder | 1. 建流语句使用 session_window + ignore_expired = 0 + partition by tbname； 1. 写入 N 批数据； 1. 写入乱序数据； 1. 对比流批结果； | 流批结果相同 | pass |
| 41 | scalar_function + update | 1. 建流语句使用 session_window + ignore_update = 0 + partition by tbname； 1. 写入 N 批数据； 1. 更新某条数据； 1. 对比流批结果； | 流批结果相同 | pass |
| 42 | scalar_function + delete | 1. 建流语句使用 session_window + ignore_expired = 0 + partition by tbname； 1. 写入 N 批数据； 1. 删除某条数据； 1. 对比流批结果； | 流批结果相同 | pass |
| 43、【基础用例】 | scalar_function + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | 流批结果相同 | pass |
| 44 | scalar_function + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | snode 覆盖所有用例，不影响原逻辑 | pass |
| 45、【基础用例】 | window_close + partition by tbname | 1. 建流语句使用 window_close + partition by tbname； 1. 写入 N 批数据并对比流批结果； | 流比批少一条 | pass |
| 46、【基础用例】 | window_close + disorder | 1. 建流语句使用 window_close + ignore_expired = 0 + partition by tbname； 1. 写入 N 批数据； 1. 乱序写入数据； 1. 对比流批结果； | 乱序数据可以被重算，流比批少一条 | pass |
| 47 | window_close + update | 1. 建流语句使用 window_close + ignore_update = 0 + partition by tbname； 1. 写入 N 批数据； 1. 更新某条数据； 1. 对比流批结果； | 更新数据可以增量计算，流比批少一条 | pass |
| 48 | window_close + delete | 1. 建流语句使用 window_close + ignore_expired = 0 + partition by tbname； 1. 写入 N 批数据； 1. 删除某条数据； 1. 对比流批结果； | 删除数据可以被重算，流比批少一条 | pass |
| 49 | window_close + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | 可以计算历史数据，流比批少一条 | pass |
| 50 | window_close + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | snode 覆盖所有用例，不影响原逻辑 | pass |
| 51 | window_close + subtable | 1. 建流语句使用 subtable； 1. 写入数据并确认 subtable 是否成功创建并写入数据； | subtable 成功创建并写入数据 | pass |
| 52、【基础用例】 | max_delay + partition by tbname | 1. 建流语句使用 max_delay + partition by tbname； 1. 写入 N 批数据并对比流批结果； | 最多等待 max_delay 时间后，流批结果相同 | pass |
| 53、【基础用例】 | max_delay + disorder | 1. 建流语句使用 max_delay + ignore_expired = 0 + partition by tbname； 1. 写入 N 批数据； 1. 乱序写入某条数据； 1. 对比流批结果； | 最多等待 max_delay 时间后，流批结果相同 | pass |
| 54 | max_delay + update | 1. 建流语句使用 max_delay + ignore_update = 0 + partition by tbname； 1. 写入 N 批数据； 1. 更新某条数据； 1. 对比流批结果； | 最多等待 max_delay 时间后，流批结果相同 | pass |
| 55 | max_delay + delete | 1. 建流语句使用 max_delay + ignore_expired = 0 + partition by tbname； 1. 写入 N 批数据； 1. 删除某条数据； 1. 对比流批结果； | 最多等待 max_delay 时间后，流批结果相同 | pass |
| 56 | max_delay + fill_history | 1. 写入一批历史数据； 1. 建流语句指定 fill_history=1； 1. 写入 N 批数据（历史数据之后的时间戳）并对比流批结果； | 最多等待 max_delay 时间后，流批结果相同 | pass |
| 57 | max_delay + snode | 1. 建 snode； 1. 建流； 1. 写入 N 批数据并对比流批结果； | snode 覆盖所有用例，不影响原逻辑 | pass |
| 58 | max_delay + subtable | 1. 建流语句使用 subtable； 1. 写入数据并确认 subtable 是否成功创建并写入数据； | subtable 成功创建并写入数据 | pass |
