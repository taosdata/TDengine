# 写入功能测试用例

测试用例中无特殊声明，创建的表均定义复合主键
***超级表语法***
```sql {wrap}
INSERT INTO meters(tbname, location, groupId, ts, current, voltage, phase) 
                values('d31001', 'California.SanFrancisco', 2, '2021-07-13 14:06:34.630', 10.2, 219, 0.32) 
                ('d31001', 'California.SanFrancisco', 2, '2021-07-13 14:06:35.779', 10.15, 217, 0.33)
                ('d31002', NULL, 2, '2021-07-13 14:06:34.255', 10.15, 217, 0.33)        
```

***自动建表语法***
```sql {wrap}
INSERT INTO d21001 USING meters TAGS ('California.SanFrancisco', 2) VALUES ('2021-07-13 14:06:32.272', 10.2, 219, 0.32);
```



| 测试类型 | 测试场景 | 用例No. | 测试用例名称 | 测试步骤 | 测试结果 | automated | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 子表-通过csv文件插入数据 | 1. 创建不带PK的超级表 1. 创建超级表的子表 1. 通过csv文件插入数据 1. 查询数据正确 | Pass | Y |  |
| 2 | 普通表-通过csv文件插入数据 | 1. 创建普通表 1. 通过csv文件插入数据 1. 查询数据正确 | Pass | Y |  |
| 3 | 超级表-插入数据 | 1. 创建不带PK的超级表 1. 创建超级表的子表 1. 通过***超级表语法***向子表插入数据 1. 查询数据正确 | Pass | Y |  |
| 4 | 子表-插入数据 | 1. 创建不带PK的超级表 1. 创建超级表的子表 1. 子表插入数据 1. 查询数据正确 1. 通过***自动建表语法***向子表插入数据 1. 查询数据正确 | Pass | Y |  |
| 5 | 普通表-插入数据 | 1. 创建不带PK的普通表 1. 插入数据 1. 查询数据正确 | Pass | Y |  |
| 6 | 子表-通过csv文件插入数据 | 1. 创建不带PK的超级表 1. 创建超级表的子表 1. 通过csv文件插入数据 1. 查询数据正确 | Pass | Y |  |
| 7 | 普通表-通过csv文件插入数据 | 1. 创建普通表 1. 通过csv文件插入数据 1. 查询数据正确 | Pass | Y |  |
| 8 | 超级表-插入数据 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过***超级表语法***向子表插入数据 1. 查询数据正确 | Pass | Y |  |
| 9 | 子表-插入数据 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表插入数据 1. 查询数据正确 1. 通过***自动建表语法***向子表插入数据 1. 查询数据正确 | Pass | Y |  |
| 10 | 普通表-插入数据 | 1. 创建带PK的普通表 1. 插入数据 1. 查询数据正确 | Pass | Y |  |
| 11 | 超级表-插入数据主键为null | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过***超级表语法***向子表插入数据，PK值为null，报错 | Pass | Y |  |
| 12 | 子表-插入数据主键为null | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表插入数据，PK值为null，报错 1. 通过***自动建表语法***向子表插入数据，PK值为null，报错 | Pass | Y |  |
| 13 | 普通表-插入数据主键为null | 1. 创建带PK的普通表 1. 插入数据，PK值为null，报错 | Pass | Y |  |
| 14 | 子表-通过文件插入数据，部分数据中复合主键列为null | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过csv文件向子表插入数据，部分PK值为null，报错 ```sql {wrap} INSERT INTO d1001 FILE '/tmp/csvfile.csv'; ``` | Pass | Y |  |
| 15 | 普通表-通过文件插入数据，部分数据中复合主键列为null | 1. 创建带PK的普通表 1. 通过csv文件向表插入数据，部分PK值为null，报错 ```sql {wrap} INSERT INTO d1001 FILE '/tmp/csvfile.csv'; ``` | Pass | Y |  |
| 16 | 超级表-插入数据主键为空 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过***超级表语法***向子表插入数据，PK值为null，报错 | Pass | Y |  |
| 17 | 子表-插入数据主键为空 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表插入数据，PK值为null，报错 1. 通过***自动建表语法***向子表插入数据，PK值为null，报错 | Pass | Y |  |
| 18 | 普通表-插入数据主键为空 | 1. 创建带PK的普通表 1. 插入数据，PK值为null，报错 | Pass | Y |  |
| 19 | 子表-通过文件插入数据，部分数据中复合主键列为空 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过csv文件向子表插入数据，部分PK值为空字符，报错 ```sql {wrap} INSERT INTO d1001 FILE '/tmp/csvfile.csv'; ``` | Pass | Y |  |
| 20 | 普通表-通过文件插入数据，部分数据中复合主键列为空 | 1. 创建带PK的普通表 1. 通过csv文件向表插入数据，部分PK值为空字符，报错 ```sql {wrap} INSERT INTO d1001 FILE '/tmp/csvfile.csv'; ``` | Pass | Y |  |
| 21 | 超级表-插入数据主键为NONE | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过***超级表语法***向子表插入数据，PK值为none，报错 | Pass | Y |  |
| 22 | 子表-插入数据主键为空NONE | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表插入数据，PK值为none，报错 1. 通过***自动建表语法***向子表插入数据，PK值为none，报错 | Pass | Y |  |
| 23 | 普通表-插入数据主键为NONE | 1. 创建带PK的普通表 1. 插入数据，PK值为none，报错 | Pass | Y |  |
| 24 | 子表-通过文件插入数据，部分数据中复合主键列为NONE | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 通过csv文件向子表插入数据，部分PK值为none，报错 ```sql {wrap} INSERT INTO d1001 FILE '/tmp/csvfile.csv'; ``` | Pass | Y |  |
| 25 | 普通表-通过文件插入数据，部分数据中复合主键列为NONE | 1. 创建带PK的普通表 1. 通过csv文件向表插入数据，部分PK值为none，报错 ```sql {wrap} INSERT INTO d1001 FILE '/tmp/csvfile.csv'; ``` | Pass | Y |  |
| 26 | 超级表-插入1条数据，缺少复合主键列对应的值 | 1. 创建带PK的超级表 1. 通过***超级表语法***插入部分数据列数据，不包含pk列，报错 | Pass | Y |  |
| 27 | 子表-插入1条数据，缺少复合主键列对应的值 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表部分列插入部分数据列数据，不包含pk列，报错 1. 通过***自动建表语法***向子表插入部分数据列数据，不包含pk列，报错 | Pass | Y |  |
| 28 | 普通表-插入1条数据，缺少复合主键列对应的值 | 1. 创建普通表 1. 表部分列插入部分数据列数据，不包含pk列，报错 | Pass | Y |  |
| 29 | 超级表-插入1条数据，有复合主键值，但缺少ts列对应的值 | 1. 创建带PK的超级表 1. 通过***超级表语法***插入数据，不包含ts列，报错 | Pass | Y |  |
| 30 | 子表-插入1条数据，有复合主键值，但缺少ts列对应的值 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表部分列插入数据，不包含ts列，报错 1. 通过***自动建表语法***向子表插入数据，不包含ts列，报错 | Pass | Y |  |
| 31 | 普通表-插入1条数据，有复合主键值，但缺少ts列对应的值 | 1. 创建普通表 1. 表部分列插入数据，不包含ts列，报错 | Pass | Y |  |
| 32 | 超级表-插入数据，仅定义ts和复合主键的值 | 1. 创建带PK的超级表 1. 通过***超级表语法***插入数据，仅包含ts和pk列，插入成功，其他数据列为null | Pass | Y |  |
| 33 | 子表-插入数据，仅定义ts和复合主键的值 | 1. 创建带PK的超级表 1. 创建超级表的子表 1. 子表部分列插入数据，仅包含ts和pk列，，插入成功，其他数据列为null 1. 通过***自动建表语法***向子表插入数据，仅包含ts和pk列，，插入成功，其他数据列为null | Pass | Y |  |
| 34 | 普通表-插入数据，仅定义ts和复合主键的值 | 1. 创建普通表 1. 表部分列插入数据，仅包含ts和pk列，，插入成功，其他数据列为null | Pass | Y |  |
| 35 | schemaless写入 - influxDB 行协议 | 1. 先创建定义了pk的超级表 1. 写入数据报错 https://docs.taosdata.com/develop/insert-data/influxdb-line/ | Pass | Y |  |
| 36 | schemaless写入 - OpenTSDB行协议 | 1. 先创建定义了pk的超级表 1. 写入数据报错 https://docs.taosdata.com/develop/insert-data/opentsdb-telnet/ | Pass | Y |  |
| 37 | schemaless写入 - OpenTSDB JSON格式协议 | 1. 先创建定义了pk的超级表 1. 写入数据报错 https://docs.taosdata.com/develop/insert-data/opentsdb-json/ | Pass | Y |  |
| 向多个表插入数据,部分表插入异常 | 38 | 向多个表插入数据 | 1. 创建一个超级表s1及其子表t1 1. 创建一个超级表s2及其子表t2 1. 创建一个普通表t3 1. 同时向t1，t2和t3插入数据，分别验证三张表因pk键数据为null的场景，所有表数据插入失败 ```sql {wrap} INSERT INTO t1 VALUES ('2021-07-13 14:06:34.630', 10.2, 219, 0.32) ('2021-07-13 14:06:35.779', 10.15, 217, 0.33) t2 (ts, current, phase) VALUES ('2021-07-13 14:06:34.255', 10.27, 0.31) t3 (ts, current, phase) VALUES ('2021-07-13 14:06:34.255', 10.27, 0.31); ``` | Pass | Y |  |
| 主键表数据导入非主键表 | 39 | 具有 primary key 的表向无 primary key 的表中写入数据 | 1. 表d001无复合主键 1. 表d002有复合主键，其他列定义相同 1. 通过 insert into...select 将表d002的数据插入到d001，报错 | Pass | Y |  |
| 40 | 子表-无 primary key 的表中写入数据向具有 primary key 的表写数据 | 1. 子表d001有复合主键 1. 表d002无复合主键，其他列定义相同 1. 通过 insert into...select 将表d002的数据插入到d001 | Pass | Y |  |
| 41 | 普通表-无 primary key 的表中写入数据向具有 primary key 的表写数据 | 1. 表d001有复合主键 1. 表d002无复合主键，其他列定义相同 1. 通过 insert into...select 将表d002的数据插入到d001 | Pass | Y |  |
|  |  | PK包含null | 1. 表d001有复合主键 1. 表d002有复合主键 1. TS重复，PK包含null，通过 insert into...select 将表d002的数据插入到d001，报错 | Pass | Y |  |
| 42 | 无重复数据 | 1. 表d001有复合主键 1. 表d002有复合主键 1. TS+PK无重复数据，通过 insert into...select 将表d002的数据插入到d001 | Pass | Y |  |
| 43 | TS重复，PK部分重复 | 1. 表d001有复合主键 1. 表d002有复合主键 1. TS重复，PK部分重复，通过 insert into...select 将表d002的数据插入到d001，插入PK不重复部分的数据 | Pass | Y |  |
| 44 | TS不重复，PK重复 | 1. 表d001有复合主键 1. 表d002有复合主键 1. TS不重复，PK重复，通过 insert into...select 将表d002的数据插入到d001，插入d002所有数据 | Pass | Y |  |
| 45 | TS重复，PK重复 | 1. 表d001有复合主键 1. 表d002有复合主键 1. TS重复，PK重复，通过 insert into...select 将表d002的数据插入到d001，插入0行数据 | Pass | Y |  |
| 46 | 超级表-插入数据ts相同，复合主键不同，新增数据 | 1. 创建超级表 1. 通过***超级表语法***向子表插入数据 1. 通过***超级表语法***向子表在插入数据，ts值相同，PK值不同，数据不会覆盖 | Pass | Y |  |
| 47 | 子表-插入数据ts相同，复合主键不同，新增数据 | 1. 创建超级表及其子表 1. 向子表插入数据 1. 向子表插入数据，ts值相同，PK值不同，数据不会覆盖 1. 通过***自动建表语法***向子表插入数据，ts值相同，PK值不同，数据不会覆盖 | Pass | Y |  |
| 48 | 普通表-插入数据ts相同，复合主键不同，新增数据 | 1. 创建普通表 1. 插入一条数据 1. 在插入一条数据，ts值相同，PK值不同，数据不会覆盖 | Pass | Y |  |
| 49 | 超级表-插入数据ts和复合主键都相同，更新数据 | 1. 创建超级表 1. 通过***超级表语法***向子表插入数据 1. 通过***超级表语法***向子表在插入数据，ts值相同，PK值相同，数据覆盖 | Pass | Y |  |
| 50 | 子表-插入数据ts和复合主键都相同，更新数据 | 1. 创建超级表及其子表 1. 向子表插入数据 1. 向子表插入数据，ts值相同，PK值相同，数据覆盖 1. 通过***自动建表语法***向子表插入数据，ts值相同，PK值相同，数据覆盖 | Pass | Y |  |
| 51 | 普通表-插入数据ts和复合主键都相同，更新数据 | 1. 创建普通表 1. 插入一条数据 1. 在插入一条数据，ts值相同，PK值相同，数据覆盖 | Pass | Y |  |
| 52 | 超级表-插入数据ts不同，复合主键相同，新增数据 | 1. 创建超级表 1. 通过***超级表语法***向子表插入数据 1. 通过***超级表语法***向子表在插入数据，ts值不同，PK值相同，数据不会覆盖 | Pass | Y |  |
| 53 | 子表-插入数据ts不同，复合主键相同，新增数据 | 1. 创建超级表及其子表 1. 向子表插入数据 1. 向子表插入数据，ts值不同，PK值相同，数据不会覆盖 1. 通过***自动建表语法***向子表插入数据，ts值不同，PK值相同，数据不会覆盖 | Pass | Y |  |
| 54 | 普通表-插入数据ts不同，复合主键相同，新增数据 | 1. 创建普通表 1. 插入一条数据 1. 在插入一条数据，ts值不同，PK值相同，数据不会覆盖 | Pass | Y |  |
| ts+复合主键+部分数据列，插入成功 | 55 | ts+复合主键+部分数据列，插入成功 | 1. 表d1001有复合主键和5个数据列 1. 插入数据包含ts、复合主键和3个数据列 | Pass | Y |  |
| 56 | 向多个表（子表或普通表）插入记录，部分表有复合主键，插入成功 | 1. 表d1001为超级表的子表且有复合主键，表d1002为普通表且无复合主键 1. 通过 INSERT INTO 同时向2张表插入数据，表d1001的所有列插入数据，表d1002部分数据列插入数据 | Pass | Y |  |
| 58 | 通过***自动建表语法***，多个表插入数据，插入成功 | INSERT INTO d21001 USING meters TAGS ('California.SanFrancisco', 2) VALUES ('2021-07-13 14:06:34.630', 10.2, 219, 0.32) ('2021-07-13 14:06:35.779', 10.15, 217, 0.33) d21002 USING meters (groupId) TAGS (2) VALUES ('2021-07-13 14:06:34.255', 10.15, 217, 0.33) d21003 USING meters (groupId) TAGS (2) (ts, current, phase) VALUES ('2021-07-13 14:06:34.255', 10.27, 0.31); | Pass | Y |  |
| 59 | 通过文件插入数据，插入成功 | 1. 表d1001为超级表的子表且有复合主键，表d1002为普通表且无复合主键 1. 插入数据 INSERT INTO d21001 USING meters TAGS ('California.SanFrancisco', 2) FILE '/tmp/csvfile_21001.csv' d21002 USING meters (groupId) TAGS (2) FILE '/tmp/csvfile_21002.csv'; | Pass | Y |  |
| 60 | 通过文件插入数据，插入成功 | INSERT INTO d1001 FILE '/tmp/csvfile.csv'; | Pass | Y |  |
| 61 | 通过文件插入数据并自动建表，部分表有复合主键，插入成功 | 1. 表d1001为超级表的子表且有复合主键，表d1002为普通表且无复合主键 1. 插入数据 INSERT INTO d21001 USING meters TAGS ('California.SanFrancisco', 2) FILE '/tmp/csvfile_21001.csv' d21002 USING meters (groupId) TAGS (2) FILE '/tmp/csvfile_21002.csv'; | Pass |  |  |
|  | 62 | bind_param单行插入 | 1. 创建超级表，定义pk 1. 插入ts1对应的5条数据，pk值是倒序 1. 插入ts2对应的5条数据，pk值是倒序，ts1>ts2 1. 插入成功 1. 查询order by ts，pk返回数据顺序正确 | Wait for test | Y |
|  | 63 | bind_param_batch多行批量插入 | 1. 创建超级表，定义pk 1. 插入ts1对应的5条数据，pk值是倒序 1. 插入ts2对应的5条数据，pk值是倒序，ts1>ts2 1. 插入成功 1. 查询order by ts，pk返回数据顺序正确 | Wait for test | Y |
|  | 64 | INT PK 插入数据到 VARCHAR PK | 1. Int长度>varchar长度，插入成功，数据截断 1. Int长度<=varchar长度，插入成功 | Pass | Y |  |
|  | 65 | VARCHAR PK 插入数据到 INT PK | 字符串仅包含正数，插入成功 | Pass | Y |  |
|  | 66 | VARCHAR(5) PK 插入数据到 VARCHAR(10) PK | 插入成功 | Pass | Y |  |
|  | 67 | VARCHAR(10) PK 插入数据到 VARCHAR(5) PK | 插入成功，数据做截断 | Pass | Y |  |
|  | 69 | int32 PK 插入数据到 int64 PK | 在不超出int范围的前提，插入成功 | Pass | Y |  |
|  | 70 | int64 PK 插入数据到 int32 PK | 在不超出int范围的前提，插入成功 | Pass | Y |  |
|  | 71 | uint32 PK 插入数据到 int32 PK | 在不超出int范围的前提，插入成功 | Pass | Y |  |
|  | 72 | int32 PK 插入数据到 uint32 PK | 在不超出int范围的前提，插入成功 | Pass | Y |  |
|  | 73 | uint64 PK 插入数据到 int64 PK | 在不超出int范围的前提，插入成功 | Pass | Y |  |
|  | 74 | int64 PK 插入数据到 uint64 PK | 在不超出int范围的前提，插入成功 | Pass | Y |  |
