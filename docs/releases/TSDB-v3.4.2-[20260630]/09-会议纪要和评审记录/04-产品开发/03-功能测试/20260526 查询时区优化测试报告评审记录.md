# 20260526 查询时区优化测试报告评审记录

## 1. 评审信息

1. 评审目的：评估 "时区与查询改造测试报告" 的合理性、安全性、性能、兼容性及可维护性
2. 评审文档：[查询时区优化 TS](../../../06-功能测试/查询时区优化%20TS.md)
3. 会议主持：关胜亮
4. 会议人员：关胜亮、霍琳贺、潘魏、张天毅、张心治
5. 会议时间：2026-05-26 10:30 - 11:00
6. 会议形式：线下
7. 会议地点：taosX
8. 会议记录人：关胜亮

## 2. 评审记录

评审团队对测试文档（时区与查询改造功能测试相关）进行了全面审查，认为所有设计合理、内容详实、流程规范，具体评审意见如下：
1. 测试目标明确：针对时区与查询改造功能（F1-F11）开展专项测试，目标聚焦配置与会话行为（SET TIMEZONE/SET FIRST_DAY_OF_WEEK/TIMEZONE() 函数/ALTER LOCAL 层级关系/连接隔离）、标量函数与 DST（TO_ISO8601/TO_CHAR/TIMETRUNCATE 的 IANA 时区参数/自然单位 n/q/y/单位倍数校验/1w firstDayOfWeek 对齐/WEEK 系列函数/DST 春跳秋退/JOIN ON TIMETRUNCATE DST 感知日对齐）、时间窗口（INTERVAL 自然单位/周窗口/季度窗口/session timezone 优先级隔离/supertable 路径）、配置路径统一（ALTER LOCAL timezone 校验规则/Windows 名称兼容/GMT 拒绝/固定偏移 POSIX 语义），同时覆盖异常场景（非法时区/非法 firstDayOfWeek/N>1 倍数拒绝/不存在 topic/模糊缩写拒绝），定位清晰、重点突出。
2. 测试用例设计全面：4 个测试文件合计 16+ 个测试类——配置与会话行为覆盖 TestSetTimezone/TestSetFirstDayOfWeek/TestTimezoneFunc/TestDisplayTimezone/TestWhereCastJoinTz/TestTodayNowTz/TestIntervalTimezone 7 大类（合法 IANA 与固定偏移边界/非法输入错误码/同连接多次切换/reconnect 隔离/ALTER LOCAL 仅影响新连接/L2→L3→L5 回退链/TODAY 跟随连接时区/NOW 回归兼容/interval 跨时区桶划分）；标量函数与 DST 覆盖 TestToIso8601Iana/TestToCharTimezone/TestTimetruncateTz/TestTimetruncateNaturalUnits/TestTimetruncateUnitMultiplierValidation/TestTimetruncateWeek/TestWeekFunctions/TestSessionFirstDayOfWeekFunction/TestDstEdge/TestToIso8601FixedOffset/TestJoinDst 11 大类（L1 覆盖 L2/固定偏移 legacy vs POSIX 双路径/N>1 全单位拒绝/DST 春跳秋退/merge join 与 hash join 一致性）；时间窗口覆盖 TestIntervalNatural/TestIntervalWeek/TestIntervalQuarter 3 大类（1n/1d/1y/1q 桶数边界/1q==3n 等价/fdow 0-6 全范围/SLIDING 组合/supertable 路径/DST no-drift）；配置路径统一覆盖 TestTimezoneConfigUnification 1 大类（IANA/裸固定偏移/UTC±/Windows 名称接受与 GMT/模糊缩写/越界/垃圾输入拒绝/固定偏移 POSIX 语义验证），用例设计科学合理、覆盖全面。
3. 测试覆盖维度完整：涵盖功能覆盖（L1-L5 优先级回退链矩阵覆盖 11 个场景）、错误码覆盖（0x26B2 非法时区/0x26B3 非法 firstDayOfWeek/0x2803 非法函数参数/0x0119 非 client-scope 配置共 4 个错误码）、高风险变更回归覆盖（7 项：TIMETRUNCATE 1w fdow 对齐/INTERVAL 1w fdow 对齐/INTERVAL 1d 时区边界/TIMEZONE() 会话可见性/固定偏移路径语义混淆/DST JOIN 匹配/ALTER LOCAL 连接快照污染）、DST 边界覆盖（春跳缺失时刻/秋退重叠时刻的写入与查询行为）、JOIN 路径覆盖（默认 merge join 与 hash_join hint 两条路径结果一致性）、平台兼容覆盖（Windows 整体跳过标记/Windows 时区名称接受），测试严谨性强。
4. 测试方法规范：明确各功能模块测试要点，详细列出测试文件/类/方法的映射关系，清晰区分正常场景（合法 IANA/固定偏移/自然单位对齐）与异常场景（非法输入/模糊缩写/N>1 拒绝/spring-gap 服务端拒绝），提供完整的优先级回退链覆盖矩阵表和高风险变更回归对照表，采用多终端协作验证实时行为（SET TIMEZONE 后 SELECT ts 展示变化），测试流程规范，结果可验证、可追溯。
5. 测试结论数据充分：全部功能测试用例通过，覆盖目标包含新增功能全部场景（F1-F11 的配置/语法/函数/窗口/对齐全路径）和异常场景，优先级回退链矩阵 11 个场景全部有对应覆盖类，7 项高风险变更均有专门回归测试类对应，结论客观真实，具备参考价值。
6. 文档信息完整：包含修订记录（3 版：1.0 初稿→1.1 同步 hash_join 支持→1.2 增补 ALTER LOCAL 统一校验与固定偏移兼容语义）、文档范围（明确 4 个测试文件边界）、测试文件清单与映射（文件→类→主题完整对照表）、关键覆盖点（4 大类详细展开每个测试类的覆盖内容）、回退链与优先级覆盖矩阵（11 行）、错误码覆盖（4 个）、高风险变更项与对应回归（7 项），修订记录清晰，逻辑连贯、格式规范，便于后续查阅与维护。

## 3. 评审结论

测试文档整体合格，符合测试文档规范要求，同意归档。

## 4. 后续行动项

无
