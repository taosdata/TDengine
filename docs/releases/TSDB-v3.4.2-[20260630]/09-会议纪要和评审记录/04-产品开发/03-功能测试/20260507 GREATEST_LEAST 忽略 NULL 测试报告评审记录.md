# 20260507 GREATEST_LEAST 忽略 NULL 测试报告评审记录

## 1. 评审信息

1. 评审目的：评估 "GREATEST / LEAST 函数忽略 NULL 测试报告" 的合理性、安全性、性能、兼容性及可维护性
2. 评审文档：[GREATEST / LEAST 函数忽略 NULL TS](../../../06-功能测试/GREATEST_LEAST%20忽略%20NULL%20TS.md)
3. 会议主持：关胜亮
4. 会议人员：关胜亮、霍琳贺、金明磊、肖波、陈伟灿、邝金清
5. 会议时间：2026-05-07 09:10 - 09:20
6. 会议形式：线下
7. 会议地点：taosX
8. 会议记录人：关胜亮

## 2. 评审记录

评审团队对测试文档（GREATEST / LEAST 函数忽略 NULL 功能测试相关）进行了全面审查，认为所有设计合理、内容详实、流程规范，具体评审意见如下：
1. 测试目标明确：针对 GREATEST/LEAST 标量函数及 v3.4.2 新增 `ignoreNullInGreatest` 配置项开展专项测试，目标聚焦基本语义（数值/字符串最大最小值）、类型推导（全数值提升/全字符串/混合类型受 `compareAsStrInGreatest` 控制）、NULL 传播（默认遇 NULL 返回 NULL）、`ignoreNullInGreatest` 全部行为（默认兼容/忽略 NULL 模式/全 NULL/与 `compareAsStrInGreatest` 正交性）、列与标量广播、不支持范围（参数个数不足/不可比较类型/行数不匹配）及性能回归，定位清晰、重点突出。
2. 测试用例设计全面：共 47 条用例分九大模块系统覆盖——基本功能 GREATEST（数值/浮点/负数/字符串，6 条）、基本功能 LEAST（数值/浮点/字符串/全负数，4 条）、NULL 处理（含 NULL 字面量/全 NULL/列含 NULL 行，4 条）、类型推导（INT/BIGINT/DOUBLE 提升/VARCHAR 长度/NCHAR 混合，4 条）、配置项 `compareAsStrInGreatest`（默认字符串比较/切换数值比较/返回类型/切换恢复，5 条）、列与表查询（多列/列+标量广播/WHERE 使用/GREATEST+LEAST 同时投影，4 条）、不支持范围（仅一个参数/无参数/LEAST 仅一个参数/不可比较类型，4 条）、边界与规模用例（空表+全 NULL 列/50 列入参/常量与多行列混合广播，3 条）、`ignoreNullInGreatest` 新增（默认兼容/忽略常量 NULL/忽略列 NULL/全 NULL/正交性/字符串类型/边界单值/单列剩余行级 NULL/类型化常量 NULL 广播/NULL 字面量多行列，12 条）、性能回归（默认配置无退化验证，1 条），用例设计科学合理、覆盖全面。
3. 测试覆盖维度完整：涵盖功能覆盖（FS §4 行为说明、§4.6 配置项含 `ignoreNullInGreatest` 全覆盖）、类型覆盖（FS §4.4 返回类型推导表每行至少一条用例）、不支持范围覆盖（参数个数 < 2、不可比较类型、行数不匹配）和 RS §7.5 测试需求矩阵 8 项全覆盖（含第 8 条性能回归）四个维度，特别关注 `ignoreNullInGreatest=1` 时 NULL 字面量与列 NULL 的区分（翻译阶段剔除 vs 执行阶段跳过）、`CAST(NULL AS INT)` 类型化常量 NULL 的广播安全性、NULL 字面量多行列驱动行数计算等深层边界，测试严谨性强。
4. 测试方法规范：明确各功能模块测试要点，详细列出用例编号、测试描述及预期结果，清晰区分正常场景（值正确性验证）和异常场景（参数类型错误/参数个数不足报错）的验证重点，配置项切换场景通过 `ALTER LOCAL` 动态验证即时生效无需重启，性能回归场景设定 100K 行表 + 30s 上限 + 3 倍容忍阈值的量化标准，测试流程规范，结果可验证、可追溯。
5. 测试结论数据充分：全部 47 条用例已自动化并通过 ASan 模式验证（0 ASan 错误、0 内存泄漏），三个测试文件分别覆盖基础功能、`ignoreNullInGreatest` 新增和性能回归，为后续自动化回归提供了稳定可扩展的全覆盖基线，结论客观真实，具备参考价值。
6. 文档信息完整：包含修订记录、测试目标（5 项目标清晰列出）、参考文档（FS/RS/在线文档）、测试结论（含三个测试文件路径和覆盖目标明细）、测试环境（OS/版本/关键配置/测试框架）、功能测试（9 个子模块）等关键信息，修订记录清晰，逻辑连贯、格式规范，便于后续查阅与维护。

## 3. 评审结论

测试文档整体合格，符合测试文档规范要求，同意归档。

## 4. 后续行动项

无
