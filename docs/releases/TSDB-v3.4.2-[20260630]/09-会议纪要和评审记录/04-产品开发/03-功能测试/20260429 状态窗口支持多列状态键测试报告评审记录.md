# 20260429 状态窗口支持多列状态键 测试报告评审记录

## 1. 评审信息

1. 评审目的：评估 "状态窗口支持多列状态键测试报告" 的合理性、安全性、性能、兼容性及可维护性
2. 评审文档：[状态窗口支持多列状态键 TS](../../../06-功能测试/状态窗口支持多列状态键%20TS.md)
3. 会议主持：关胜亮
4. 会议人员：关胜亮、霍琳贺、张天毅、肖波、陶建辉、潘魏
5. 会议时间：2026-04-29 09:15 - 09:30
6. 会议形式：线下
7. 会议地点：taosX
8. 会议记录人：关胜亮

## 2. 评审记录

评审团队对测试文档（状态窗口支持多列状态键功能测试相关）进行了全面审查，认为所有设计合理、内容详实、流程规范，具体评审意见如下：
1. 测试目标明确：针对 STATE_WINDOW 从单列扩展到多列状态键后的查询与流计算语义开展专项测试，目标聚焦语法解析（单列历史写法/关键字写法兼容、多列仅子句形式、混用报错）、计划生成（多列表达式键计划构造、虚拟表窗口优化基线）、查询执行器（多类型组合切窗、聚合伪列、PARTITION BY tbname、表达式键）、NULL/EXTEND/ZEROTH_STATE 语义（按列独立维护上一有效状态键、候选段归属决策、三路径一致性）、流式触发与状态通知（多列载荷 JSON 数组化、首窗口 prevState 为 JSON null）、虚拟表回归与跨版本兼容性六大层次，定位清晰、重点突出。
2. 测试用例设计全面：覆盖 SQL 语法与兼容性（单列关键字写法/历史写法/混用报错/多列表达式语法/非法输入，5 条单元测试）、计划生成与重写（多列表达式键/单列兼容/聚合联合/虚拟表基线更新与优化回归，5 条）、多列查询能力（基础双列切窗/多类型组合/聚合伪列/分区列重写/表达式键/单列回归，6 条自动化用例已接入持续集成）、空值边界与零状态（多列空值基线/三种 EXTEND 模式/零状态过滤/部分空值回归/历史回归问题/查询实时历史三路径一致性/前后兼容边界候选段，7 条自动化用例已接入持续集成）、流式触发与状态通知（表达式键触发与历史回放/多列状态通知载荷/单列通知兼容/JSON null 语义/事件序列正确性，5 条自动化用例）、虚拟表回归（vtable/vstable/vctable 结果基线与计划基线，4 条）、跨版本兼容性（3.3.x~3.4.1.0 升级验证/旧格式流式任务与 TSMA 兼容/CI 注册，3 条），七大模块共 35 条用例，用例设计科学合理、覆盖全面。
3. 测试覆盖维度完整：涵盖语法解析、计划生成、查询执行器、流式触发、状态通知、虚拟表和兼容性六个层次，功能测试从语法解析到执行器结果层层递进，空值语义覆盖全 NULL 行/部分 NULL 候选段/按列独立比较/旧状态污染/未定义列误判零状态/待定状态污染等历史回归问题，流式通知覆盖多列 JSON 数组化与单列兼容并行验证，虚拟表覆盖三种模式（mode 0/1/2）下普通表/超级表/子表的结果基线与计划基线，兼容性覆盖 3.3.3.0~3.4.1.0 多版本升级路径，测试严谨性强。
4. 测试方法规范：明确各功能模块测试要点，详细列出测试用例、测试描述及测试结果，清晰区分单元测试覆盖（C++ parser/planner 测试）、自动化覆盖已接入持续集成（pytest 查询/空值/流式用例）、基线覆盖（虚拟表 .in/.ans 文件）和非默认持续集成自动化覆盖（状态通知/兼容性用例）四种覆盖状态，测试入口和运行命令清晰记录，测试流程规范，结果可验证、可追溯。
5. 测试结论数据充分：七大模块 35 条用例全部通过或已有自动化覆盖，核心查询与空值语义用例已接入默认持续集成，状态通知与跨版本兼容性用例保留为非默认持续集成入口，测试设计与自动化覆盖已就绪，结论客观真实，具备参考价值。
6. 文档信息完整：包含修订记录、测试目标、参考文档（含 9 个测试入口文件路径）、测试环境、测试结论、功能测试（6 个子模块）、长期稳定性测试观察项建议、性能测试与安全测试边界说明、兼容性测试等关键信息，修订记录清晰，逻辑连贯、格式规范，便于后续查阅与维护。

## 3. 评审结论

测试文档整体合格，符合测试文档规范要求，同意归档。

## 4. 后续行动项

无
