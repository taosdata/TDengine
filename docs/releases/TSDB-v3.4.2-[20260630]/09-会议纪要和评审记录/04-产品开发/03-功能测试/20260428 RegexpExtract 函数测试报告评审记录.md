# 20260428 RegexpExtract 函数测试报告评审记录

## 1. 评审信息

1. 评审目的：评估 "REGEXP_EXTRACT 函数测试报告" 的合理性、安全性、性能、兼容性及可维护性
2. 评审文档：[REGEXP_EXTRACT 函数 TS](../../../06-功能测试/RegexpExtract%20函数%20TS.md)
3. 会议主持：关胜亮
4. 会议人员：关胜亮、霍琳贺、金明磊、肖波、李亚强、邝金清
5. 会议时间：2026-04-28 16:15 - 16:30
6. 会议形式：线下
7. 会议地点：taosX
8. 会议记录人：关胜亮

## 2. 评审记录

评审团队对测试文档（REGEXP_EXTRACT 函数功能测试相关）进行了全面审查，认为所有设计合理、内容详实、流程规范，具体评审意见如下：
1. 测试目标明确：针对 REGEXP_EXTRACT 标量函数开展专项测试，目标聚焦 FS 定义的全部行为（默认 group_idx=1、group_idx=0 完整匹配、多捕获组、NULL/无匹配/超出范围返回、空串语义、按行求值、WHERE/子查询上下文、NCHAR 类型），同时覆盖参数约束验证（pattern 须为常量、group_idx 范围 0-512）和所有不支持范围（参数数量错误、非字符串 str、pattern 列引用、负数或超过 512 的 group_idx、无效正则表达式），定位清晰、重点突出。
2. 测试用例设计全面：共 40 条用例分十二大模块系统覆盖——基本功能（单/多捕获组/无捕获组/VARCHAR 输入，4 条）、group_idx=0 完整匹配（无捕获组/有捕获组/无匹配，3 条）、多捕获组索引（group 1/2/超出组数，3 条）、NULL 与无匹配（str NULL/无匹配/多次匹配只取第一个/group_idx=0 NULL/group_idx NULL/非参与捕获组/pattern NULL，8 条）、空串场景（捕获组匹配空串/空输入串，2 条）、表查询与多行（多行按行求值/含 NULL 列/空表，3 条）、WHERE 子句（IS NOT NULL 过滤/等值过滤，2 条）、NCHAR 类型（与 VARCHAR 等价验证，1 条）、嵌套子查询与 GROUP BY（子查询结果用于 GROUP BY，1 条）、不支持范围（参数过少/过多/非字符串类型/pattern 列引用/负数 group_idx/无效正则/group_idx 超最大值，7 条）、正则表达式特性（字符类/锚点/大小写敏感，3 条）、文档示例验证（年份提取/完整匹配/无匹配，3 条），用例设计科学合理、覆盖全面。
3. 测试覆盖维度完整：涵盖功能覆盖（FS §4 行为说明、§4.3 参数规则、§4.4 返回值语义、§4.7 边界场景全覆盖）和不支持范围覆盖两个维度，特别关注空串与 NULL 的区分（捕获组匹配空串返回空字符串而非 NULL）、非参与捕获组返回 NULL 等易混淆边界，返回类型正确性标注为架构确认项处理务实，测试严谨性强。
4. 测试方法规范：明确各功能模块测试要点，详细列出用例编号、测试描述及预期结果，清晰区分正常场景（提取值正确性验证）和异常场景（参数类型错误、无效正则报错、group_idx 越界报错）的验证重点，文档示例验证模块逐条执行用户手册中示例查询确保文档与实现一致，测试流程规范，结果可验证、可追溯。
5. 测试结论数据充分：测试规格文档共设计 40 条用例，全部已执行通过（test_fun_sca_regexp_extract.py），覆盖目标明确列出功能覆盖和不支持范围覆盖两个维度，为后续自动化回归提供了稳定可扩展的全覆盖基线，结论客观真实，具备参考价值。
6. 文档信息完整：包含修订记录、测试目标、参考文档、测试环境（含 OS、版本、测试框架和测试文件路径）、测试结论等关键信息，修订记录清晰，逻辑连贯、格式规范，便于后续查阅与维护。

## 3. 评审结论

测试文档整体合格，符合测试文档规范要求，同意归档。

## 4. 后续行动项

无
