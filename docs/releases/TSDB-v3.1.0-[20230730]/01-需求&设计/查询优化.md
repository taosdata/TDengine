# 查询优化

@潘魏
TD-23677        确定的wal文件清理策略后，需要同时修改create db、show db等命令的操作和显示
测试目标：show create database和系统表中不存在不对外显示的参数。
Tester: @郭向阳

TD-25003        SHOW INDEXES/SHOW TAGS/SHOW TABLE TAGS支持两种语法格式
测试目标：验证两种语法都支持，功能一致。
Tester: @郭向阳

TS-2289        3.0 show dnode <dnode_id> variables显示有误
TS-2288        3.0 show local variables参数有误
测试目标：验证所有配置项都在这两个命令结果中显示，同时显示其作用节点。
Tester: @郭向阳

TS-2500        show create database information_schema;
测试目标：验证所有系统库支持show create database命令，显示结果为最简语句。
Tester: @郭向阳

TD-24745        嵌套查询外层udf输入为常量表时，需要在服务器端添加调用逻辑
测试目标：验证支持JIRA中场景查询，结果正确。
Tester: @郭向阳

TD-24992        create sma增加last timestamp参数
测试目标：原有功能不受影响即可。
Tester: @段宽军

TD-25006        order by + limit进一步优化
测试目标：验证非主键排序性能变化，无论是有无Limit性能都应当有提升。
Tester: @冯超
【Charles】完成测试，[【Test report】对order by limit的查询时间性能再次优化](https://taosdata.feishu.cn/wiki/GlVPwyaJ2igfhPkfY17c1NSmn9d) 

TD-23763        [美团]超级表last_row + partition by tbname + slimit性能差
测试目标：验证partition by tbname + slimit性能都应当有提升。
Tester: @郭向阳

TD-22415        [3.0][grant] 企业版授权根据系统时间判定存在的漏洞优化
测试目标：验证恶意修改系统时间场景下实际授权时间不受影响。
Tester: @冯超
【Charles】等开发完成交付测试

TD-25083        将 taosKeeper 所建 log 库从 license 检查的测试点数量中排除
测试目标：验证log库所含表不在授权测点范围内。
Tester: @冯超
【Chrles】测试完成，待客户验证

TD-24987        [grant] 3.0 授权码增加发放时间，基于发放时间生效。
测试目标：验证一个集群存在多个授权时实际生效授权以最新一次授权时间为准。
Tester: @冯超
【Charles】等开发完成交付测试

TD-24920        限制开源版只支持有限的操作系统和 CPU 架构
测试目标：验证所有已支持平台功能不受影响，在所有不支持平台上运行报错。
Tester: @冯超

TS-2412        3.0 【查询->投影查询->排序】issue 板块用户倒排序+LIMIT 3W条数据需要5秒
测试目标：验证JIRA中场景查询性能是否有提升。
Tester: @郭向阳

TD-25056        优化 select * limit order by的内存使用中 16GB内存占满及一定vgroups数量下，内存随子表数增加而下降
测试目标：验证JIRA中场景下查询内存受控，不会OOM。
Tester: @郭向阳

TS-3587        [中石化EPDC]order by第一列时间戳速度过慢
测试目标：验证JIRA中场景查询性能是否有提升。
Tester: @郭向阳

TD-24871        [美团]查询疑似内存泄漏
测试目标：验证JIRA中场景下查询内存受控，不会OOM。
Tester: @郭向阳

TD-25072        支持随机调度和再调度策略
测试目标：验证单个节点(dnode)负载高的场景下查询不会都被阻塞执行。
Tester: @郭向阳
