# 优化Schemaless 子表名生成逻辑

TD-19291

- 增加一个配置  smlAutoChildTableNameDelimiter，让用户配置子表名tag之间的分隔符。
- 不能硬编码为 - ，不然如果用户tag里包含 - ，就会有问题，所以开放给用户配置。
- 子表名生成新规则为 `标签1分隔符标签2分隔符···` ，在文档里说明。同时保留旧规则，兼容之前的写入。
- 如果拼接的子表名长度超过表名限制，会放弃该表名生成规则，使用md5规则
- 收益：用户不需要在每条记录中附带子表名的tag，只需要在 taosc.cfg 中配置即可
