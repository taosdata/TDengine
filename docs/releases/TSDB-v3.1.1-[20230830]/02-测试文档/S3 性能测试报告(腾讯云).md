# S3 性能测试报告(腾讯云)

引用： [COS对象存储用户手册](https://taosdata.feishu.cn/wiki/OjmwwCmqdiENPckuo4icjb5fnzc) 

## 一、目的

     S3 存储的优势是空间大，存储成本低，所以我们把 TDengine 多级存储中最后一级存储内的数据存放在 S3 上，可以极大降低用户的存储成本。

## 二、实现原理

    在 TDengine 存储文件中，.data 文件存放时序数据，占用空间最大，所以仅把多级存储中最后一级中的 .data 文件存储在 S3 上，其它小文件仍然原地不动，这样即简化了功能实现，又降低了存储成本。
    当配置了 S3 存储后，多级存储中第二级向第三级迁移时，.data 文件会移动至 S3, 本地存储空间将被释放出来。
    当查询需要获取 S3 上的数据时，会从S3 上下载相应在 DATA 文件到第三级存储配置的本地磁盘上，实现数据查询，如果本地资源不够时，会删除在第三级存储中访问时间离目前最远的 .data 文件来释放空间。下载机制会充分利用第三级存储可用的磁盘空间，如果磁盘空间不足则查询会失败。

## 三、测试说明 

###       1、写入速度

        因为多级存储中咱们约定是不允许在第三级上写入数据的，数据写入只发生在第一级和第二级上，第一级与第二级的写入都是本地磁盘，所以写入性能与原来一样。

### 2、查询速度分类

  1）查询时间范围只在第一级和第二级上的查询速度与原来一致
  2）查询时间范围包括第三级上的数据时，查询时间 = 原来的查询时间 + 下载 DATA 文件所需时间    

## 四、测试数据

###      1、测试环境

     为了达到良好的带宽，这里选择了腾讯云主机进行了测试：

####     1) 硬件配置

      **网络宽带**
      从腾讯云主机到腾讯云对象存储的下载速度（1G文件下载）实测为：
          下载速度 ： 40M/s
     **CPU：**8C 2699Mhz
     **MEMORY: **16G
     **硬盘：  **50G 

####   2）数据库生成   

     子表列数： 315列 bigint , 4 列 timestamp, 2 个标签值
     子表数：  1000
     写入频率： 1个/秒
     多级存储 KEEP:  1d, 2d, 5d 
     DURATION:   1d
     副本数： 1
     写入开始时间： 2023-08-27 16:00:00
     写入结束时间： 2023-09-13 14:14:55
     总数据量：    2.9 亿条记录，测点数为 931 亿点
     写入数据天数： 16天
     占用空间：        41G
     每个 data 文件大小： 平均 400-600M 左右

### 2、测试方法及步骤

**  1）写入数据**
     使用 taosBenchmark 把数据一次性写入完成，约需要2小时左右，一级和二级存储上也都写入了数据  
     记录第一次性能数据
      S3 服务器 0 个 data 文件
  **2）1 天后的二级迁移至 S3 服务器**
     因为配置的 DURATION 为 1 天, 所以需要等待 24小时后，会有二级存储的 4 个data文件(配置的 4 个VGROUP)迁移到 S3 服务器
     数据迁移至 S3 服务器后，本地应释放出于迁移出相同大小的磁盘空间出来
     同时原来在一级的 data 文件将有 4 个迁移至 二级存储
     记录第二次性能数据
     S3 服务器 4 个 data 文件
**3）2 天后第二批数据迁移至 S3**
     2天后第二批数据迁移至 S3 服务器，同时逐级依次也会发生迁移    
     记录第三次性能数据
     S3 服务器 8 个 data 文件
**4）3 天后第二批数据迁移至 S3**
     3 天后第三批数据迁移至 S3 服务器，同时逐级依次也会发生迁移    
     记录第四次性能数据
     S3 服务器 12 个 data 文件

### 3、测试结果

**性能测试查询语句设计：**

| 1 | 纯S3 上的查询 | 投影 | select * from dxtest where ts>='2023-08-27 16:00:00' and ts < '2023-08-28 00:00:00'; |
| --- | --- | --- | --- |
|  |  | 聚合 | select count(ts) from dxtest where ts>='2023-08-27 16:00:00' and ts < '2023-08-28 00:00:00'; |
| 2 | S3( 25%) + 本地(75%) 组合查 | 投影 | select * from dxtest where ts>'2023-08-27 16:00:00' and ts < '2023-08-29 00:00:00' |
|  |  | 聚合 | select count(ts) from dxtest where ts>'2023-08-27 16:00:00' and ts < '2023-08-29 00:00:00' |
| 3 | 整体查询 | 聚合 | select count(ts) from dxtest; |
| 4 | First 首行查询 | 投影 | select first(*) from dxtest; |
| 5 | Last 未行查询 | 投影 | select last(*) from dxtest; |


**测试数据：**

| 序号 | 查询 SQL |  | 第一次查询 | 第二次查询 | 返回数量 |
| --- | --- | --- | --- | --- | --- |
| 1 | 纯 S3 上的数据查询 | 投影查询 | 40.2s | 31.5s | 576万 |
|  |  | 聚合查询 | 9.86s | 0.01s | 576万 |
| 2 | S3( 25%) + 本地(75%) 组合查 | 投影查询 | 137.3s | 129.2s | 2303万 |
|  |  | 聚合查询 | 9.7s | 0.03s | 2303万 |
| 3 | 整体查询 | 聚合查询 | 1.23s | 1.21s | 2.9亿 |
| 4 | First 首行查询 | 投影查询 | 9.26s | 0.06s | 1 |
| 5 | Last 未行查询 | 投影查询 | 0.99s | 0.95s | 1 |

  说明
    1）每次第一次查询前都删除本地缓存S3上的 data 文件，第二次查询不删除本地缓存
    2）因为表是宽列，有 321 列，所以投影查询全部数据返回很耗时
    3）对于大数据量的投影查询，下载文件所占用的时间在整个时间占用中影响有限

查询一 纯 S3 上的投影查询分不同列对性能影响如下：

|  | 1列 | 10列 | 10列 limit10,10 | 100列 | 100列 limit10,10 | 全部列 |
| --- | --- | --- | --- | --- | --- | --- |
| 第一次查询 | 9.03s | 9.63s | 8.17s | 18.25s | 9.31s | 40.2s |
| 第二次查询 | 0.48s | 1.21s | 0.006s | 9.5s | 0.008s | 31.5s |
| 两次差值 | 8.55s | 8.41s | 8.164s | 8.75s | 9.302s | 8.7s |

      说明： 
           1） 查询返回的列数不同对查询时长影响还是比较大的
           2）从两次查询查差可以看出，这个差值即固定的下载文件所耗的时长，比较固定
          

磁盘占用情况, 每天本地磁盘空间都会被释放出来，直到最后全部数据迁移至 S3 ，本地磁盘全部被释放

| 序号 | 查询 SQL |  | 第 1 天 | 第 2 天 | 第 3 天 | 第 4 天 | 第 5 天 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  | 本地磁盘占用 | 41G | 39G |  |  |  |
|  | 本地剩余空间 | 2G | 3G |  |  |  |
|  | S3 空间占用 | 0 | 1G |  |  |  |

      说明：
              基本符合预期，迁移至 S3 的数据在本地空间得到了释放

## 五、性能总结        

###      1、查询性能由下载文件大小及速度确定

     在使用 S3 做为最后一级存储中，首次查询的性能主要由要下载文件的大小及下载速度两因素决定，下载文件越小，下载速度越快，查询性能越好

###      2、再次查询性能与原来一样

      下载只发生在首次查询中，所以只影响首次查询，再次查询或查询只涉及到前面查询相同的 data 文件时，查询速度与原来一样快。

###      3、规划好 data 文件的大小

       数据库配置参数 DURATION 配置的天数决定了 data 文件的大小，所以项目中要根据应用的读写频率，规划好 data 文件大小，根据自己的带宽情况决定最佳的 DATA 文件大小，也不建议过小，过于小的话也影响 TDengine 在一级和二级存储上的查询效率。
      如果在 1M 之内的带宽，建议 DATA 文件在 200M 以下
      1 ~ 10M 带宽的，建议 DATA 文件在 300 M 左右
      10~50 M带宽的，建议 DATA 文件在 500 M 左右      
      50~100 M 带宽的， 建议 DATA 文件在1G 内都合适   

###      4、S3上的查询不是所有查询都能支持

       最后一级存放到 S3 上，并非除了慢所有查询都能实现，如 S3 上有 100T 数据，本地磁盘只有 100G 空间，如果做全时间段内查询，需要把 100T 都拉下来进行过滤，但本地只有 100G 空间，咱们查询不支持拉一部分过滤一部分，需要全拉下来才能开始过滤，所以这样的查询是无法实现的。解决办法只能把原来的一个查询按时间切分成几个小查询来完成，最后再自己汇总。   

### 5、历史数据尽量点查或分段查

       因为查询需要先下载后查询，下载速度一般都比较慢，所以对于存放到最后一级在S3上的历史数据最好能够指定时间点查或分小时间段来查，查询速度相对来说可接受一些，如果在整个范围内查询，可能会花很长很长的时间。
       同时说明，有需求要存储在 S3 上数据的用户，一般都是数据量非常庞大，但偶尔会有一些历史查询统计场景，可以使用这套系统，如果是频繁在 S3 上查询的需求，查询性能主要依赖下载速度，S3 一般都在公有云上，下载速度不可能很快，所以对于频繁大范围查询历史数据的用户，不适合使用这套 S3 系统。
