# 企业版 3.1 版本Release Notes

1. 3.1.1.25
   - 优化：拆分 sync 的 RPC 传输通道
   - 数组删除元素导致的死锁
2. 3.1.1.24
   - 读取数据块中的内存泄露
   - 授权部分 crash
   - root 密码变更后通过 taosAdapter 写入失败
3. 3.1.1.23
   - 优化： order by ts desc limit 1 性能提升
4. 3.1.1.22
   - 在删除数据并进行了compact 和 split vgroup 操作后，查询产生 crash
   - status 同步线程有可能被 memory trim 操作阻塞
   - status 同步线程在 epset 更新后未保存更新后的 epset，导致每次都需要一次不必要的重试
   - 查询  count(*) 导致 "Sync Leader Unreachable" 错误
   - show queries 导致  taosd crash
   - 向客户端返回了不正确的错误码导致查询不能被完全释放
   - TMQ 同步卡住
5. 3.1.1.21
   - 连接耗尽后系统服务阻塞
6. 3.1.1.20
   - 断电重启后产生 WAL 不一致
7. 3.1.1.19
   - INT 类型字段写入浮点数报错
   - 查询从不同客户端上已经删除的列导致的 crash
8. 3.1.1.18
9. 3.1.1.17
10. 3.1.1.16
   - 删除数据后 last()/last_row() 结果不正确
11. 3.1.1.15
   - timetruncate() 按周截断结果错误
12. 3.1.1.14
   - 使用 insert into 批量导入 CSV 文件导致 taosd crash
   - 从边端到云端的数据复制与同步工作异常
13. 3.1.1.13
   - 带筛选条件的查询加上 \G 后报错
   - to_iso8601() 返回结果错误
14. 3.1.1.12
   - 数据删除后last()/last_row() 仍然能够查询到
   - 数据重整对写入产生了阻塞
15. 3.1.1.11
   - 消息同步处理机制导致对集群中 dnode 状态判断错误
16. 3.1.1.10
17. 3.1.1.9
18. 3.1.1.8
19. 3.1.1.7
   - 数据重整及时清理中间文件
20. 3.1.1.6
   - interval fill() 返回值错误
   - taosAdapter 
21. 3.1.1.5
22. 3.1.1.4
