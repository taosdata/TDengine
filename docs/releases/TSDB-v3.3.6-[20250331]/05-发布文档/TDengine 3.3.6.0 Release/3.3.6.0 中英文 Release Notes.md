# 3.3.6.0 中英文 Release Notes

### 1. 中文

```markdown

### 2. 特性

   - TS-4244  特性：支持 Decimal 数据类型
   - TS-5080  特性：客户端兼容性优化
   - TS-5255  特性：支持通过 cols 关键字获取单行选择函数返回结果所在行的其他列值
   - TS-5579  特性：增加海讯数据库适配支持
   - TS-5992  特性：支持将常量值作为连接主键
   - TS-6168  特性: 将参数 minFreeDiskSize 的最大值更改为2TB
   - TS-5386  特性：支持通过 KEEP 参数控制超级表的时序数据保存时长
   - TS-5806  特性：taosX 支持备份文件到 S3 存储
   - TD-32931 特性：JDBC 客户端高效写入
   - TD-33098 特性：taosX 异常数据处理优化
   - TD-33335 特性：MQTT 数据源本地持久
   - TD-33860 特性：支持接入可视化客户端库 Perspective
   - TD-33947 特性：WebSocket 接口支持 validate_sql 
   - TS-4894  特性：使用 ODBC 集成 Tableau
   - TS-4897  特性：支持虚拟表
   - TS-5089  特性：taosbechmark 支持生成CSV文件
   - TS-5279  特性：TDengine 接入帆软 BI
   - TS-5467  特性：支持在流计算中使用虚拟表
   - TS-5468  特性：流计算支持 continue_window_close 触发模式
   - TS-5469  特性：流计算支持窗口事件通知机制
   - TS-5470  特性：流计算支持为事件窗口和状态窗口设置最小持续时长限制
   - TS-5607  特性：支持 Greatest、Least 函数
   - TS-5614  特性：为运算符 match 提供别名 regexp
   - TS-5891  特性：Explorer 支持 Decimal 数据类型
   - TS-5915  特性：Rust 连接器支持 Decimal 类型
   - TS-6016  特性：taosX/Agent 支持 HTTPS 连接
   - TD-30906 特性：Explorer 支持 SQL 格式化
   - TS-5726  特性：taosX 支持数据源任务导出和导入
   - TS-5728  特性：OPC-DA 支持轮询开始时间作为主键列

### 3. 优化

   - TD-33798 优化：当订阅主题启用 only meta 选项时，数据订阅接口将不再传输时序数据
   - TS-5445  优化：支持重新整理 tdb 文件
   - TS-5617  优化: 异步执行流创建操作以缩短响应时间
   - TS-5755  优化：taosX Kafka 写入性能提升
   - TS-5766  优化：支持为 nchar 和 varchar 类型的标签字段创建索引
   - TS-5776  优化：大幅提升集群之间的数据同步性能
   - TS-5794  优化：多级存储中同一层级数据文件在多个磁盘中的分布
   - TD-32013 优化：连接器支持订阅新参数
   - TD-33165 优化：提高自动建表的写入性能
   - TD-33349 优化：taosX 订阅任务在WAL 保留时长为 0 时报错
   - TD-33528 优化：Explorer 错误信息优化
   - TD-33557 优化：优化 MQTT 数据源接收性能
   - TD-33599 优化：C# 连接器异步接收 WebSocket 返回
   - TD-33615 优化：河北电力 Transform 支持外部过滤时间戳
   - TD-33662 优化：taosBenchmark 支持 WebSocket STMT 写入
   - TD-33668 优化：增加双副本数据库的错误排查信息
   - TD-33733 优化：taosAdapter 优化消息订阅机制
   - TS-5486  优化：Interp 函数支持插值时间范围
   - TS-5584  优化：支持通过 SQL 查看 Vnode 恢复进度
   - TS-5606  优化：双活命令行启动可同步新增数据库
   - TS-5613  优化：支持在 CAST 函数中使用判断表达式
   - TS-5650  优化：支持在 SHOW VARIABLES 系列命令中使用 LIKE 关键字，实现对变量名称的模糊匹配
   - TS-5784  优化：Explorer: root 用户可查看和修改所有用户创建的任务
   - TS-5785  优化：OPCDA 支持按 {id} {/tagname} {_id} 提取 Item ID
   - TS-5805  优化：为双副本集群增加 assign leader 命令，解决 WAL 版本较高节点无法启动导致的集群恢复问题。
   - TS-5837  优化：单节点故障后双副本集群恢复服务的时长
   - TS-5876  优化：重构 STMT 代码结构，显著提升查询与写入性能
   - TS-5889  优化：taosBechmark 高可靠写入优化
   - TS-5927  优化：密码最大长度扩展到 255 位
   - TS-5928  优化：消费者订阅数据时可设置最小拉取条数和最长等待时间
   - TS-5966  优化：opc 日志响应配置文件中 log 配置
   - TS-5990  优化：C WebSocket 连接器支持 session.timeout.ms 
   - TS-6000  优化：各组件支持长密码及特殊字符
   - TS-6036  优化：JDBC 订阅消费者参数支持动态添加
   - TS-6037  优化：Python 订阅消费者参数支持动态添加
   - TS-6038  优化：连接器支持可变订阅消费者参数
   - TS-6064  优化：MQTT 未指定 SSL 失败时自动尝试 SSL 连接
   - TS-6076  优化：在 show disk_info 结果中，为压缩率数值添加百分号（%）
   - TS-6088  优化：在企业版中增加巡检工具
   - TS-6137  优化：流计算在 force_window_close 模式下支持滑动窗口
   - TS-6164  优化：支持在卸载 tar 安装包时通过参数 -e 实现静默卸载
   - TD-26215 优化：提高降采样和按标签分组查询的性能
   - TS-5631  优化：重复启动 taosX 报错
   - TS-5743  优化：添加夏令时使用指南
   - TS-5797  优化：优化 taosX OPC 数据源CPU和内存占用
   - TS-6072  优化：Explorer 表格显示时间戳使用固定精度
   - TS-6115  优化：支持删除存在活跃消费者的主题

### 4. 修复

   - TD-32218 修复：删除包含大量子表的超级表时可能出现的内存不足问题
   - TD-33439 修复：taosX 归档文档读取
   - TD-33555 修复：Explorer 密码不合法时应报错
   - TD-33665 修复：在 stmt2 的 interlace 写入模式下，异步执行返回的行数存在错误
   - TD-33675 修复：Explorer 显示备份任务异常原因
   - TD-33872 修复：dnode 过载情况下 vnode 可能离线的问题
   - TD-33961 修复：在 VGroup 主节点切换时，流计算可能出现大量事务积压，从而导致计算延迟
   - TD-34042 修复：Explorer 社区版版本号错误
   - TD-34090 修复：Node.js 连接器支持密码包含特殊字符
   - TS-5944  修复：taosBenchmark 随机值超出设置范围
   - TS-6055  修复：OPCUA 连接器 Windows 系统错误 10053
   - TS-6084  修复：Adapter 或 Nginx 重启某些情况下导致 taosX Panic
   - TS-6116  修复：配置了多级存储的集群，show disk_info 命令的结果不正确
   - TS-6134  修复：show <database>.disk_info 执行结果中的文字显示错误
   - TS-6136  修复：在查询超级表时，若同时使用 ORDER BY 和 LIMIT，可能会返回一条非预期的记录
   - TS-6169  修复：2.x 数据源包含不支持的健康检查参数
   - TD-33632 修复：备份计划下次执行时间不允许设置为过去的时间
   - TS-5980  修复：旧版本备份任务兼容性
   - TS-6058  修复：在对多个嵌套查询语句执行UNION操作时，查询可能会出现错误。
   - TS-6079  修复：在状态窗口查询时，PARTITION BY 和 SELECT 子句中若存在相同表达式，会导致 taosd 崩溃
   - TS-6080  修复：当子查询未返回结果且其中包含 LIMIT 1 关键词时，可能导致父查询的结果出现异常或不准确
   - TS-6117  修复：当表名中包含 * 时，taosc 会出现崩溃问题。
   - TS-6145  修复：在 Windows 平台下，使用 JDBC PreparedStatement 写入数据时，关闭 PreparedStatement 可能导致 JVM 崩溃
   - TS-6149  修复：使用 Websocket 参数绑定批量写入数据时，子表名含中文会产生乱码

```

### 5. English

```markdown

### 6. Features

   - TS-4244  feat: supports Decimal data type
   - TS-5080  feat: improve compatibility of taosc
   - TS-5255  feat: support retrieving the values of other columns in the row returned by the single-row selection function through the cols keyword.
   - TS-5579  feat: integrate with haixun database api
   - TS-5992  feat: support constant as join primary key
   - TS-6168  feat: change the max value of minFreeDiskSize to 2TB
   - TS-5386  feat: supports control the retention period of time-series data in the supertable via the KEEP parameter.
   - TS-5806  feat: support backup to s3 storage in taosX
   - TD-32931 feat: effient data writing in JDBC
   - TD-33098 feat: improve error handling of taosX
   - TD-33335 feat: support persistent cache to guarantee data completement 
   - TD-33860 feat: interate with perspective BI library
   - TD-33947 feat: support taos_validate_sql in websocket
   - TS-4894  feat: integrate Tableau with ODBC driver
   - TS-4897  feat: supports virtual table
   - TS-5089  feat: taosbechmark can generate csv files
   - TS-5279  feat: TDengine now integrate with FineBI
   - TS-5467  feat: supports the use of virtual tables in stream computing
   - TS-5468  feat: stream computing supports the continue_window_close trigger mode.
   - TS-5469  feat: stream computing supports window event notification
   - TS-5470  feat: stream computing support setting a minimum duration limit for event window and state window.
   - TS-5607  feat: support for greatest and least Functions
   - TS-5614  feat: provide an alias regexp for the operator match.
   - TS-5891  feat: experimental decimal data types support in explorer
   - TS-5915  feat: support decimal types in rust connector
   - TS-6016  feat: taosX/agent support https connection
   - TD-30906 feat: explorer add support for sql formatter
   - TS-5726  feat: taosX support export/import data-in tasks
   - TS-5728  feat: support request ts as primary key in opc-da

### 7. Enhancements

   - TD-33798 enh: when the topic enables the "only meta" option, the data subscription interface will no longer transmit time-series data.
   - TS-5445  enh: support compact TDB files.
   - TS-5617  enh: execute stream creation asynchronously to reduce response time.
   - TS-5755  enh: improve kafka performance in taosX
   - TS-5766  enh: support index creation for tag fields of nchar and varchar types.
   - TS-5776  enh: significantly enhance the data synchronization performance between clusters
   - TS-5794  enh: optimize the distribution of data files across multiple disks within the same tier in multi-level storage
   - TD-32013 enh: connectors support new subscription params
   - TD-33165 enh: improve the write performance of automatic table creation
   - TD-33349 enh: taosX should raise error when WAL retention period is 0 
   - TD-33528 enh: improve error messages in explorer
   - TD-33557 enh: improve performance of mqtt fetching messages
   - TD-33599 enh: c# connector add async receiver for websocket connection
   - TD-33615 enh: filter timestamp by configuration for hebei power transform
   - TD-33662 enh: suppport stmt via websocket in taosBenchmark
   - TD-33668 enh: increase the error troubleshooting information for dual-replica databases
   - TD-33733 enh: improve consumer polling strategy in  ws(taosAdapter)
   - TS-5486  enh: Interp function supports interpolation over a time range.
   - TS-5584  enh: support for monitoring vnode recovery progress through SQL
   - TS-5606  enh: tracking new database with replica cli
   - TS-5613  enh: support the use of conditional expressions in the CAST function
   - TS-5650  enh: supports using the LIKE keyword in the SHOW VARIABLES series commands to enable pattern matching for variable names.
   - TS-5784  enh: root can view/edit all tasks in explorer
   - TS-5785  enh: OPCDA datasource table template can use  {/tagname}{id}{_id} extractor
   - TS-5805  enh: introduce the "assign leader" command for dual-replica clusters to address the issue of cluster recovery when a dnode with a higher WAL version fails to start.
   - TS-5837  enh: optimize the service recovery time of a dual-replica cluster after a single-node failure
   - TS-5876  enh: refactor the STMT code to significantly enhance query and write performance.
   - TS-5889  enh: improve benchmark writing robotics
   - TS-5927  enh: extend the maximum password length to 255 characters.
   - TS-5928  enh: when subscribe data, consumers can set the minimum rows and maximum waittime.
   - TS-5966  enh: respect to log.* settings in configuration for opc logs
   - TS-5990  enh: C taosws support session.timeout.ms
   - TS-6000  enh: support long passwords with special characters in all components
   - TS-6036  enh: support additional subscription parameters in JDBC
   - TS-6037  enh: Python consumer can subscribe topic using newly added parameters
   - TS-6038  enh: allow newly added subscription params without updating connectors 
   - TS-6064  enh: MQTT fallback to ssl when tcp failed
   - TS-6076  enh: add a percentage sign (%) after the compression ratio value in the results displayed by show disk_info.
   - TS-6088  enh: includes inspection tools in the Enterprise Edition.
   - TS-6137  enh: stream computing supports sliding window in the force_window_close mode
   - TS-6164  enh: supports silent uninstallation of the tar package using the -e parameter.
   - TD-26215 enh: improve the efficiency of downsampling and group by tag queries
   - TS-5631  enh: taosX does not allow start repeatedly
   - TS-5743  enh: add DST(Daylight Saving Time) guideline
   - TS-5797  enh: improve cpu/mem performance in OPC data-in
   - TS-6072  enh: use fixed seconds format in explorer table view
   - TS-6115  enh: support deletion of topics with active consumers.

### 8. Fixes

   - TD-32218 fix: OOM may occur when dropping a super table containing a large number of child-tables.
   - TD-33439 fix: taosX util to read archived data
   - TD-33555 fix: explorer should raise error when password is not valid
   - TD-33665 fix: In stmt2 interlace mode, asynchronous execution returns incorrect affectrow count.
   - TD-33675 fix: explorer display error reason for backup tasks
   - TD-33872 fix: vnode may go offline when dnode is overloaded
   - TD-33961 fix: stream computing experiences a significant backlog of transactions during vgroup leader switch, resulting in computational delays.
   - TD-34042 fix: fix explorer oss --version error
   - TD-34090 fix: support special characters of password in Node.js
   - TS-5944  fix: random value out of range in taosBenchmark
   - TS-6055  fix: fix os error 10053 on windows for OPCUA
   - TS-6084  fix: taosX panic when adapter/nginx restarted in some cases
   - TS-6116  fix: the results of the show disk_info command are incorrect when configuring multi-level storage.
   - TS-6134  fix: typo in the execution result of show <database>.disk_info
   - TS-6136  fix: when querying super table, using ORDER BY and LIMIT together may return an unexpected record.
   - TS-6169  fix: allow health params in 2.x data-in 
   - TD-33632 fix: backup next execution time does allow past time
   - TS-5980  fix: fix compatibility of backup task upgrading to latest
   - TS-6058  fix: query error when performing a UNION operation on multiple nested queries.
   - TS-6079  fix: in state window queries, if the same expression exists in both the PARTITION BY and SELECT clauses, it will cause taosd to crash.
   - TS-6080  fix: when the subquery does not return any results but contains the LIMIT 1 clause, it may cause the result of the parent query to be abnormal or inaccurate.
   - TS-6117  fix: taosc crash when table names contain `*`
   - TS-6145  fix: on windows, closing a JDBC PreparedStatement while writing data may cause the JVM to crash.
   - TS-6149  fix: when using Websocket parameter binding to batch write data, Chinese characters in subtable names will cause garbled text

```
