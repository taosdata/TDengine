# 晶澳太阳能客户场景测试

## 1、选择客户场景

场景来源：[客户场景测试用例-晶澳太阳能](https://taosdata.feishu.cn/wiki/GvCvwBtb5iIBKUkPd0qcTZFInRI)

## 2、环境搭建

在长稳测试环境建立相应测试库，关键参数：cachemodel = both,cachesize =100,vgroups =50,replica =3,count(*)>5000w; 均达到或者超过客户场景要求。
```bash {wrap}
taos> show  create database dbnew2\G;
*************************** 1.row ***************************
       Database: dbnew2
Create Database: CREATE DATABASE `dbnew2` BUFFER 256 CACHESIZE 100 CACHEMODEL 'both' COMP 2 DURATION 10d WAL_FSYNC_PERIOD 3000 MAXROWS 4096 MINROWS 100 STT_TRIGGER 2 KEEP 3650d,3650d,3650d PAGES 256 PAGESIZE 4 PRECISION 'ms' REPLICA 3 WAL_LEVEL 1 VGROUPS 50 SINGLE_STABLE 0 TABLE_PREFIX 0 TABLE_SUFFIX 0 TSDB_PAGESIZE 4 WAL_RETENTION_PERIOD 3600 WAL_RETENTION_SIZE 0 KEEP_TIME_OFFSET 0h ENCRYPT_ALGORITHM 'none' S3_CHUNKPAGES 131072 S3_KEEPLOCAL 525600m S3_COMPACT 1 COMPACT_INTERVAL 0d COMPACT_TIME_RANGE 0d,0d COMPACT_TIME_OFFSET 0h
Query OK, 1 row(s) in set (0.001325s)
```

## 3、测试sql

```bash {wrap}
select last(*) from dbnew2.stb0;
select last(ts) from dbnew2.stb0;
select last(c0) from dbnew2.stb0;
select last(c1) from dbnew2.stb0;
select last(c2) from dbnew2.stb0;
select last(c3) from dbnew2.stb0;
select last(c4) from dbnew2.stb0;
select last(c5) from dbnew2.stb0;
select last(t0) from dbnew2.stb0;
select last(t1) from dbnew2.stb0;
select last_row(*) from dbnew2.stb0;
select last_row(ts) from dbnew2.stb0;
select last_row(c0) from dbnew2.stb0;
select last_row(c1) from dbnew2.stb0;
select last_row(c2) from dbnew2.stb0;
select last_row(c3) from dbnew2.stb0;
select last_row(c4) from dbnew2.stb0;
select last_row(c5) from dbnew2.stb0;
select last_row(t0) from dbnew2.stb0;
select last_row(t1) from dbnew2.stb0;
select * from dbnew2.stb0 limit1;
```

## 4、测试结论

可以长时间运行，并返回结果。
