# 超级表支持 Keep 参数 TS

## 1. 测试目标

本次测试的主要目标：测试超级表支持 keep 参数。

## 2. 变更历史

| Date | Version | Owner | Memo |
| --- | --- | --- | --- |
| 2025-03-13 | 0.1 | @鲍之骁 | 初稿 |
|  |  |  |  |

## 3. 测试范围

1. 功能及正确性测试：测试超级表支持 keep 参数后，各种行为是否符合预期。
2. 性能问题：tsbs 查询性能与之前持平。
3. 升级和兼容性测试：测试升降级版本行为是否正确。

## 4. 测试结论

功能符合预期，测试通过。

## 5. 开发质量报告

| 统计指标 | 数量 |
| --- | --- |
| 提测被拒次数 | 0 |
| 基础测试用例不通过 | 0 |
| Bug 总数 | 0 |
| 严重 Bug 总数 | 0 |

## 6. 已知问题和限制

## 7. 测试环境

- OS: Windows, Linux, macOS
- Browser: Chrome

## 8. 测试用例

### 8.1 功能

在提测时，开发应保证基础用例全部通过。
| 类型 | 测试目的 | 测试步骤 | 预期结果 | 是否为基础用例 | 测试结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 功能测试 | 创建超级表支持设置 keep 参数，并校验范围。 | 1. 启动集群
1. 创建超级表，使用正确的 keep 参数(d,h,m)。
2. 创建超级表，使用错误的 keep 参数(错误范围，错误单位)。 | 正确使用 keep 参数可以正确执行。
错误使用 keep 参数提示错误信息。 | Y | Pass |  |
| 功能测试 | 超级表 alter 设置 keep 参数，并校验范围。 | 1. 启动集群
1. 创建超级表
2. alter 超级表使用正确的参数。
3. alter 超级表使用错误的参数（错误范围，错误单位）。 | 正确使用 keep 参数可以正确执行。
错误使用 keep 参数提示错误信息。 | Y | Pass |  |
| 功能测试 | 子表和普通表不支持 create 设置 keep 参数。 | 1. 启动集群
1. 创建超级表
2. 创建子表使用 keep 参数。
3. 创建普通表使用 keep 参数。 | 提示错误信息。 | Y | Pass |  |
| 功能测试 | 子表和普通表不支持 alter 设置 keep 参数。 | 1. 启动集群
1. 创建超级表
2. alter 子表使用 keep 参数。
3. alter 普通表使用 keep 参数。 | 提示错误信息。 | Y | Pass |  |
| 功能测试 | 只要不超过 database 的 keep ,可以写入 stb keep 时间前的数据，并且对查询可见。 | 1. 启动集群
1. 创建数据库 keep 10d
2. 创建超级表 keep 7d
3. 插入数据六天前的数据，八天前的数据
4. 数据对于查询可见 | 1. 数据写入成功
5. 数据查询结果正确 | Y | Pass |  |
| 功能测试 | 超过 stb keep 时间的数据,在 compact 时应该被删除 | 1. 启动集群
1. 创建数据库 keep 10d
2. 创建超级表 keep 7d
3. 插入数据六天前的数据，八天前的数据
4. 数据对于查询可见
5. flush database 
6. compact database 
7. 查询 | 所有执行均成功。
compact 前六天前和八天前的数据均可见。
compact 后六天前的数据可见，八天前的数据被正确删除。 | Y | Pass |  |
| 功能测试 | 使用 alter 修改keep 时间，修改后执行 compact，数据正确被删除。 | 1. 启动集群
1. 创建数据库 keep 10d
2. 创建超级表 keep 7d
3. 插入数据六天前的数据，八天前的数据
4. 数据对于查询可见
5. flush database 
6. compact database 
7. 查询
8. alter 超级表 keep 1d
9. flush database
10. compact database 
11. 查询 | 所有执行均成功。
compact 后查询结果集为空。 | Y | Pass |  |
| 兼容性测试 | 测试 taosd 升降级 | 1. 使用旧版 taosd 初始化集群 升级
1. 使用新版 taosd 初始化集群 降级 | 不存在兼容性问题 | Y | Pass |  |
| 兼容性测试 | 测试 taosd & taosc 兼容性 | 1. 使用旧版 taosc 连接新版 taosd
1. 使用新版 taosc 连接旧版 taosd | 不存在兼容性问题 | Y | Pass |  |

### 8.2 可用性

不包含可用性问题。

### 8.3 可靠性

无

### 8.4 性能

请参考文档：
[超级表支持 Keep 参数查询性能对比](https://taosdata.feishu.cn/docx/VTHPdZ52Qo8LxsxGTVMcsansn4g)

### 8.5 安全性

无安全性问题。

### 8.6 兼容性

见8，9测试例

### 8.7 本地化

不涉及本地性问题。

## 9. Jira

## 10. 风险评估

## 11. 参考文档

[超级表支持 Keep 参数 FS](https://taosdata.feishu.cn/wiki/YS1hwWB0di1GaSkShhfcY9UVnuf)
