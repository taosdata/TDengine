# 高 CPU 负载触发选主分析与测试方案（TDinternal）

## 1. 问题背景与分析范围

目标：分析在 CPU 负载非常高时，为什么会出现选主（leader 变更）现象，并给出后续可执行的测试方案。

本次分析聚焦以下代码路径：

- Sync 核心状态机与计时器：
  - community/source/libs/sync/src/syncMain.c
  - community/source/libs/sync/src/syncTimeout.c
  - community/source/libs/sync/src/syncElection.c
  - community/source/libs/sync/src/syncAppendEntries.c
  - community/source/libs/sync/src/syncRequestVote.c
  - community/source/libs/sync/src/syncRequestVoteReply.c
- Mnode 同步接入与参数：
  - community/source/dnode/mnode/impl/src/mndSync.c
  - community/source/common/src/tglobal.c
  - community/source/dnode/mnode/impl/src/mndConfig.c
  - community/include/libs/sync/sync.h


## 2. 高负载下触发选主的可能方向与原因

### 2.1 方向 A：事件处理线程/消息队列被 CPU 打满，导致心跳处理延迟，Follower 选举超时

关键代码证据：

- Sync 消息统一分发入口：syncProcessMsg
  - community/source/libs/sync/src/syncMain.c:224
- 选举超时由定时器回调投递到消息队列：syncNodeEqElectTimer
  - community/source/libs/sync/src/syncMain.c:2792
- 选举超时消息最终调用 syncNodeElect
  - community/source/libs/sync/src/syncTimeout.c:128
  - community/source/libs/sync/src/syncElection.c:81
- Follower 收到 heartbeat 或 appendEntries 后才会 reset elect timer
  - heartbeat 路径：community/source/libs/sync/src/syncMain.c:3876
  - appendEntries 路径：community/source/libs/sync/src/syncAppendEntries.c（末尾 resetElect 分支）

机制解释：

- 在 CPU 紧张时，即使网络心跳“已经到达”，也可能因为处理线程抢不到 CPU，消息不能及时进入或处理到 reset elect timer。
- 一旦 follower 在 elect timeout 窗口内没完成 reset，就会进入 candidate 并发起投票，触发选主。


### 2.2 方向 B：Leader 发送心跳本身被调度延迟，导致下游副本误判 leader 失联

关键代码证据：

- Leader 切换后启动 per-peer 心跳定时器：
  - start: community/source/libs/sync/src/syncMain.c:1864
  - 回调: community/source/libs/sync/src/syncMain.c:2876
- 心跳发送依赖定时器回调执行与消息发送成功
  - community/source/libs/sync/src/syncMain.c:2931
  - community/source/libs/sync/src/syncMain.c:2958

机制解释：

- 高 CPU 下 leader 本地定时器回调被拖延，会导致心跳间隔抖动变大。
- follower 侧选举计时是随机窗口（基线到 2 倍基线），如果连续几次心跳延迟叠加超过该窗口，就会触发 follower 竞选。


### 2.3 方向 C：重负载导致 heartbeat/appendEntries 处理慢，出现“逻辑上超时”

关键代码证据：

- heartbeat 处理路径有慢处理打点（SYNC_HEARTBEAT_SLOW_MS）
  - 阈值定义：community/include/libs/sync/sync.h:47（1500ms）
  - 处理耗时判断：community/source/libs/sync/src/syncMain.c:3769, 3854

机制解释：

- 系统虽然未完全失联，但处理耗时持续高于心跳周期，意味着“业务线程看见的心跳间隔”被放大。
- 在选举超时配置较激进时，会产生误选主。


### 2.4 方向 D：高负载叠加日志复制/快照/清理任务，放大状态机压力

关键代码证据：

- ping timeout routine 内会执行 replicate、snapshot resend、resp clean 等周期任务：
  - community/source/libs/sync/src/syncTimeout.c:58（syncNodeTimerRoutine）
  - community/source/libs/sync/src/syncTimeout.c:71（syncNodeReplicate）

机制解释：

- 当复制滞后或快照传输重发频繁时，周期任务会进一步占用 CPU，形成“心跳/选举处理被挤压”的正反馈。


### 2.5 方向 E：收到更高 term 消息导致当前 leader 主动 step down

关键代码证据：

- leader 收到 term >= currentTerm 的 heartbeat，会投递本地 step-down 命令
  - community/source/libs/sync/src/syncMain.c:3823
  - community/source/libs/sync/src/syncMain.c:3920（SYNC_LOCAL_CMD_STEP_DOWN 处理）
- appendEntries / requestVote / voteReply 也会触发 step down
  - community/source/libs/sync/src/syncAppendEntries.c
  - community/source/libs/sync/src/syncRequestVote.c:103
  - community/source/libs/sync/src/syncRequestVoteReply.c:57

机制解释：

- CPU 压力导致某节点先进入 candidate 并 bump term；其他节点即便原本是 leader，只要看到更高 term，也会降级。
- 外在表现是“高负载期间 leader 抖动频繁”，本质是 term 被连续抬升。


### 2.6 方向 F：配置窗口偏小，抗抖动能力不足

关键代码证据：

- mnode 默认：elect=3000ms, heartbeat=500ms
  - community/source/common/src/tglobal.c:156,157
- mnode 启动时直接用 tsMnodeElectIntervalMs / tsMnodeHeartbeatIntervalMs
  - community/source/dnode/mnode/impl/src/mndSync.c:513,514
- syncTimeout 会联动改写 elect/heartbeat（比例关系）
  - community/source/dnode/mnode/impl/src/mndConfig.c:960-979（注释中的公式）

机制解释：

- 选举窗口并非绝对固定，实际是随机区间 [electBaseLine, 2*electBaseLine]。
- 在高负载环境下，如果心跳周期与 elect 基线距离不够大，且队列/调度抖动明显，误触发概率会上升。


## 3. 技术测试方案（按优先级）

下面给出“先复现、再定位、再收敛”的测试矩阵。建议先做 P0/P1。

### 3.1 P0：单点 CPU 压测下的选主复现（Leader / Follower 分开压）

目的：验证“CPU 抢占导致心跳处理延迟 -> 触发选主”的主链路。

方案：

1. 3 mnode 或 3 replica vnode 集群，保持稳定写入。
2. 仅在 leader 节点施加 CPU 压力（80%、90%、95%、100% 阶梯）。
3. 仅在 follower 节点施加同样压力，和 leader 压测结果对比。
4. 观测：
   - term 是否增长
   - leader 是否切换
   - heartbeat 慢日志是否显著增加

预期：

- leader 侧高压更容易导致下游超时选举。
- follower 侧高压更容易出现“该 follower 自身先发起竞选”。


### 3.2 P0：参数扫描（elect/heartbeat 比例扫描）

目的：找出在当前硬件负载下不易误选主的参数区间。

建议矩阵：

1. 固定 heartbeat，逐步提高 elect（例如 3s -> 4s -> 6s -> 8s）。
2. 固定 elect，调大 heartbeat（例如 500ms -> 800ms -> 1s）并观察吞吐/延迟副作用。
3. 联动设置 syncTimeout，并验证 mndConfig 的公式联动是否符合预期。

结论目标：

- 得到“在 CPU x% 时，最小可接受 elect 基线”的经验曲线。


### 3.3 P1：网络抖动与 CPU 叠加（混沌场景）

目的：区分纯 CPU 问题和网络问题叠加后的敏感性。

方案：

1. 在 P0 稳定复现后，对 leader->follower 链路增加延迟、抖动、丢包。
2. 分别测试：
   - 仅网络抖动
   - 仅 CPU 高负载
   - 网络 + CPU 叠加
3. 统计 leader 切换频次与恢复时间。


### 3.4 P1：复制滞后/快照压力叠加测试

目的：验证 syncNodeTimerRoutine 内部周期任务对选主稳定性的影响。

方案：

1. 构造高写入，拉大 follower 滞后，触发复制追赶/可能快照路径。
2. 叠加 CPU 压力。
3. 对比有无滞后情况下的选主频次。

关注点：

- syncTimeout routine 调度是否变慢。
- heartbeat 发送/处理时延分布是否劣化。


### 3.5 P2：长稳与耐久性测试（24h+）

目的：避免“短测稳定、长测抖动”的误判。

方案：

1. 选择 P0/P1 下最优参数组合。
2. 持续 24h-72h 写入 + 周期性 CPU 脉冲压力。
3. 输出：
   - leader 切换次数
   - term 增速
   - 业务可用性指标（成功率、P99 延迟）


## 4. 可直接复用的现有测试资产

仓库中已有较多 cluster/sync 相关测试，可以优先复用：

- sync 与 election 相关 case：
  - community/test/cases/02-Databases/05-Sync/test_sync_3Replica1VgElect.py
  - community/test/cases/04-SuperTables/04-Query/test_stable_query_during_leader_election.py
  - community/test/cases/uncatalog/army/cluster/test_arbitrator_election.py
- mnode/vnode cluster 变更与故障切换：
  - community/test/cases/70-Cluster/test_5dnode3mnode_stop_follower_leader.py
  - community/test/cases/70-Cluster/test_5dnode3mnode_stop_loop.py
- sync 单测集合：
  - community/source/libs/sync/test/

执行层面可结合已有脚本：

- community/tests/system-test/pytest.sh
- community/tests/system-test/loop.sh

可将单个 case 放入 loop.sh 循环执行，以提高复现概率。


## 5. 建议的观测与判定标准

### 5.1 关键观测

1. role/term 变化：是否在压测窗口内连续抬升 term。
2. heartbeat 慢日志占比：是否持续超过阈值打点。
3. 超时与 stepDown 触发路径：
   - requestVote / appendEntries / heartbeat localCmd(stepDown)
4. 同步队列积压迹象：消息处理延后、定时器回调实际触发延迟。

### 5.2 判定口径

- 可接受：在目标负载下，长稳运行无异常 leader 抖动或 term 异常增长。
- 不可接受：无网络故障前提下，CPU 高负载单独即可稳定复现 leader 频繁切换。


## 6. 结论与建议

### 6.1 结论（当前代码视角）

从代码路径看，高 CPU 触发选主最可能来自以下链路：

1. 心跳发送/处理调度延迟。
2. elect timer 在未及时 reset 时触发 candidate 竞选。
3. 某节点先 bump term 后，其他节点依据更高 term step down，形成 leader 抖动。

这类问题通常不是“选举逻辑错误”，而是“时序预算被高负载吃掉”导致的分布式时钟语义退化。

### 6.2 建议优先级

1. 先做 P0（CPU 压测 + 参数扫描）拿到稳定复现和缓解区间。
2. 再做 P1（网络与复制压力叠加）确认边界。
3. 最后用 P2 长稳验证参数组合可用于生产。

如果后续测试确认问题稳定复现，可再进入下一阶段：

- 在 sync 核心增加更细粒度观测（timer 实际触发漂移、队列排队时延、heartbeat send/recv 分位数）。
- 评估是否需要对高负载场景引入更保守的 election 预算策略（例如动态放大 elect 基线）。
