# taosdump 支持虚拟表 导入/导出 - RS

## 1. 修订记录

| 编写日期 | 发布日期 | 版本 | 修订人 | 主要修改内容 |
| --- | --- | --- | --- | --- |
| 2026-01-28 | YYYY-MM-DD | 0.1 | 裴亚明 | 初始版本 |

## 2. 引言

### 2.1 术语与缩写名词

**虚拟表（Virtual Table）**：虚拟表是一种不存储实际数据而可以用于分析计算的表，数据来源为其它真实存储数据的子表、普通表，通过将各个原始表的不同列的数据按照时间戳排序、对齐、合并的方式来生成虚拟表。虚拟表是一种动态数据结构，主要用于解决时间序列数据的多表关联、对齐和整合问题。

### 2.2 相关文档资料

### 2.3 优先级要求

优先级：高

### 2.4 版本要求

开源版和企业版都需要

## 3. 需求目标

上海电气项目中，交付人员使用 taosdump 导出包含虚拟表的数据库时失败，，目前 taosdump 不支持虚拟表的导出/导入，需要开发特性以支持 导出/导入虚拟表。

## 4. 功能需求

| 序号 | **功能类别** | **功能名称** | 功能描述 |
| --- | --- | --- | --- |
| 1 | 导出 | 导出虚拟超级表 | 支持导出数据库中的虚拟超级表信息到文件中 |
| 2 | 导出 | 导出虚拟子表 | 支持导出数据库中的虚拟子表信息到文件中 |
| 3 | 导出 | 导出虚拟普通表 | 支持导出数据库中的虚拟普通表信息到文件中 |
| 4 | 导入 | 导入虚拟超级表 | 支持从文件中导入数据库中的虚拟超级表 |
| 5 | 导入 | 导入虚拟子表 | 支持从文件中导入数据库中的虚拟子表 |
| 6 | 导入 | 导入虚拟普通表 | 支持从文件中导入数据库中的虚拟普通表 |

## 5. 性能需求

导出导入虚拟表在常规时间内完成，与现有超级表、子表导入/导出性能一致。

## 6. 安全需求

不涉及

## 7. 其他需求

无
