# 3.3.8.0 考试题目

#### 1. 第一部分，笔试题目，所有人回答

1. 什么是 SMA？
2. Block-Wise-SMA 的特点和适用场景是什么？
3. Rollup-SMA 的特点和适用场景是什么？
4. Timerange-Wise-SMA 的特点和适用场景是什么？
5. TDgpt 的 Imputation 算法的作用是什么？
6. TDgpt 的 Prophet 算法作用是什么？
7. taosX 逻辑备份和恢复支持那些使用场景？
8. JDBC 如何使用多个 taosAdapter 进行负载均衡和 failover？
9. taosgen 在表达式生成数据上支持能力如何？
10. 那个连接器支持通过 ADO 接口来访问 TDengine？

#### 2. 第二部分，笔试题目，售前交付回答

1. 如何为子表的字符串类型标签指定多个值？
2. RSMA 的原理是什么？
3. TSMA 的原理是什么？
4. 给出数据补值函数示例语句，先进行窗口聚合，然后再进行缺失聚合窗口的数据补值
5. 状态窗口起止点配置的关键参数是什么？
6. 解释一下 BSS（Byte-Stream-Split）字节流分割编码

#### 3. 第三部分，实操题目，售前交付回答

1. 搭建三节点集群，使用 TLS 证书，认证加密传输
2. 创建三副本数据库，使用 taosBenchmark 写入数据，使用 BSS 编码
3. 应用新增的函数和语法进行查询
   - 聚合函数： std、variance、stddev_samp、var_samp、group_concat
   - 条件函数：if、ifnull、nvl、nullif、nvl2
   - 比较运算符：isnull、isnotnull、coalesce
   - 日期函数：date
   - 字符串函数：find_in_set、like_in_set、regexp_in_set
   - 使用 state_window 查询，通过 extend 参数设置起止点
4. 运维操作
   - 通过 scan 命令扫描时序数据文件的完整性
   - 调整 Balance Vgroup Leader 命令的执行逻辑，使 VGroup 的 Leader 分布更加均匀
