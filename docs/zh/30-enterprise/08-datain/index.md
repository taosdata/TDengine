---
sidebar_label: 数据写入
title: 数据写入
description: TDengine 数据源或第三方数据源写入 TDengine
---

TDengine 支持可以配置不同类型的数据源，包括旧版本 TDengine2， TDengine3，PI， OPC-UA，OPC-DA，InfluxDB，MQTT，Kafka，CSV 等，将它们的数据写入到当前正在被 Explorer 管理的 TDengine 集群中。

```mdx-code-block
import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

<DocCardList items={useCurrentSidebarCategory().items}/>
```
