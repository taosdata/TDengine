**高级选项** 区域默认折叠，点击右侧 `>` 可展开。消息队列类数据源（如 Kafka、Pulsar）常见项如下。

**最大读取并发数**：限制数据源连接数或读取线程数。默认 `0` 表示由采集器自动配置。

**批次大小**：单次发送的最大消息数或行数，默认常见为 `1000`。

**写入并发数量**：同时写入 TDengine 的并发任务数量。

健康监测相关选项见 [健康状态](../index.md#健康状态)。

![advanced_options.png](../assets/advanced_options.png)
