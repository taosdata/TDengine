# 2026-03-31

## 今日总结（内容具体避免空泛）

test_idmp_vehicle.py 用例偶发失败排查。
修复 heap use after free 问题数据不对问题，写入相关，rongkun 已修复编写自动化恢复 CI 失败case 环境 的skill

## 明日计划（内容具体避免空泛）

处理CI 失败case编写自动化恢复 CI 失败case 环境 的skill增加自动化分析 数据订阅CI 失败原因

## 困难、风险和障碍

*(空)*

---

# 2026-03-30

## 今日总结（内容具体避免空泛）

test_idmp_meters.py 用例偶发失败排查。
修复，CI 中test_tmq_bugs.py 用例偶发失败排查。
修复，CI 中

## 明日计划（内容具体避免空泛）

处理CI 失败case排查  test_tmq_bugs.py  用例里另一个偶发失败case

## 困难、风险和障碍

*(空)*

---

# 2026-03-27

## 今日总结（内容具体避免空泛）

排查 test_tmq_vtable.py 用例偶尔失败原因虚拟子表在客户端meta缓存没有的情况getTableMeat 时会出现 heap overflow 导致的，转panwei处理。
windows 用例失败问题处理测试验证其他用例失败是否解决，都已解决，完成合并

## 明日计划（内容具体避免空泛）

test_idmp_meters.py 用例偶发失败排查。
优化用例test_tmq_vtable.py。
处理python 中的中文注释等。

## 困难、风险和障碍

*(空)*

---

# 2026-03-26

## 今日总结（内容具体避免空泛）

排查用户中冶订阅数据消费者离线问题修复，写单侧，分别在windows 和 linux 上测试，正常，PR 中。
windows 用例失败问题处理test_tmq_raw_block_interface.py用例失败 在多个机器上测试，没复现，已合入。
test_sliding.py test_notify.py 排查，修复，PR 中tmq_split_*.py 用例失败处理，PR中

## 明日计划（内容具体避免空泛）

排查用户中冶订阅数据消费者离线问题处理CI 失败case。
windows 用例失败问题处理处理CI 失败case。

## 困难、风险和障碍

*(空)*

---

# 2026-03-25

## 今日总结（内容具体避免空泛）

排查用户中冶订阅数据消费者离线问题用户修改系统时间，导致 timer 异常，没有发送心跳到服务器，导致consumer 离线给用户提供建议，改大心跳超时间隔，并修改消费程序，consumer 离线后重连。
排查 timer 异常原因，尝试修复，测试中。
ci-main-windows：test_tmq_raw_block_interface.py用例失败修改测试用例，适配windows 平台。
本地（win
- 11）测试正常，需要在 Windows server 2022 CI 机器上复现。
tmq_stb_select.py windows 上用例失败问题排查win 11测试正常。
Windows server 2022 机器异常test 开关没开，tmq_sim 没有编译split vnode 后，表没了导致数据不对。
东明继续排查

## 明日计划（内容具体避免空泛）

排查用户中冶订阅数据消费者离线问题排查 timer 异常原因，尝试修复，测试。
ci-main-windows：test_tmq_raw_block_interface.py用例失败在 Windows server 2022 CI 机器上测试一下。
ci-main-windows：流计算结果不符合预期排查 test_sliding.py 用例失败问题

## 困难、风险和障碍

*(空)*

---

# 2026-03-24

## 今日总结（内容具体避免空泛）

测试虚拟表流计算meta 变更trigger 在wal 没数据时，打印日志太频繁trigger 在删除虚拟子表后，没有更新 tablelisttmq_subscribe_stb0.py 用例偶尔失败排查drop table 时没有更新订阅的table list ，导致订阅数据时获取meta 报错 not found。
已修复

## 明日计划（内容具体避免空泛）

排查用户中冶订阅数据消费者离线问题处理 流计算 windows 平台失败case

## 困难、风险和障碍

*(空)*

---

