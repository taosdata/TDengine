# 2026-04-08

## 今日总结（内容具体避免空泛）

[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 补充功能说明文档，主要是多分组应该扩充支持的算子[中创新航] 流计算固定值作为主键时运行报错 分析用户遇到的流运行报错，补充最小复现步骤

## 明日计划（内容具体避免空泛）

[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 继续补充功能说明文档

## 困难、风险和障碍

*(空)*

---

# 2026-04-07

## 今日总结（内容具体避免空泛）

[中创新航]流计算运行时批量插入数据taosd奔溃 PR 已合入：修复流计算语句是 from %%tbname 时 external window 算子的行为，不要把结果集都缓存在内存中，添加用例并验证用户场景的OOM问题，验证通过

## 明日计划（内容具体避免空泛）

[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 继续补充功能说明文档

## 困难、风险和障碍

*(空)*

---

# 2026-04-03

## 今日总结（内容具体避免空泛）

[中创新航]流计算运行时批量插入数据taosd奔溃：问题根因是 ext_win 算子对于多表投影的场景采用了“先全量缓存结果、再按窗口输出”的实现方式，随着窗口数量、输出列数或输出行数增加，该算子的内存开销也会线性增长，后续应该增加外存算法来控制内存占用，已创建相关问题：[流计算] 外部窗口算子在多表投影场景下可能 OOM另外，这个用户场景的计算语句是 from %%tbname，不期望走到多表处理的逻辑，目前正在修复

## 明日计划（内容具体避免空泛）

[中创新航]流计算运行时批量插入数据taosd奔溃 完成该问题的修复[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 继续补充功能说明文档

## 困难、风险和障碍

*(空)*

---

# 2026-04-02

## 今日总结（内容具体避免空泛）

[中创新航]流计算运行时批量插入数据taosd奔溃：使用 perf 分析 CPU 满载的热点函数路径，发现 CPU 实际开销很低，用户把内存负载误认为是 CPU 占用了；
使用 gperftools 采集内存分配的堆栈信息，初步分析是 ext_win 算子的非聚合路径在特定场景下把窗口结果缓存起来，当窗口数太多时，内存就会线性增长；

## 明日计划（内容具体避免空泛）

[中创新航]流计算运行时批量插入数据taosd奔溃 继续分析和修复该场景下的 OOM 问题[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 继续补充功能说明文档

## 困难、风险和障碍

*(空)*

---

# 2026-04-01

## 今日总结（内容具体避免空泛）

[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 开始写功能说明文档，完成 50%[协鑫鑫光]嵌套聚合结果与预期不符 修复 ci 失败用例，PR 已合入

## 明日计划（内容具体避免空泛）

[售前][冠德] 流计算 external window 和多分组优化支持 JOIN 语句 继续补充功能说明文档[中创新航]流计算运行时批量插入数据taosd奔溃 分析用户场景下CPU满载和 OOM 的问题

## 困难、风险和障碍

*(空)*

---

