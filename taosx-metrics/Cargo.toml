[package]
name = "taosx-metrics"
edition = "2021"
version.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
metrics = "0.22.0"
metrics-util = "0.16.0"
flume = "0.11.0"
quanta = { version = "0.12", default-features = true}
indexmap = { version = "1", default-features = true }
