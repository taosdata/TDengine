[package]
name = "restexample"
version = "0.1.0"
edition = "2021"

[dependencies]
libtaos = { version = "*", features = ["rest"] }
tokio = { version = "*", features = ["rt", "macros", "rt-multi-thread"] }
