CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(TDengine)

INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/os)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/util)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/common)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/client)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/dnode/mgmt)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/dnode/mnode)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/monitor)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/transport)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/scheduler)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/source/dnode/mnode/impl/inc)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/qcom)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/catalog)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/planner)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/nodes)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/sync)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/wal)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/tfs)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/libs/stream)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/source/dnode/vnode/src/inc)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/include/dnode/mnode/sdb)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/contrib/cJson)
INCLUDE_DIRECTORIES(${TD_ENTERPRISE_DIR}/src/inc)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/src/mnode/inc)
INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/src/dnode/inc)
INCLUDE_DIRECTORIES(inc)

LIST(APPEND LIB_SRC ./src/grantMain.c)
LIST(APPEND LIB_SRC ./src/mndGrant.c)

IF (BUILD_WITH_CFG_GRANTS)
  ADD_DEFINITIONS(-DGRANTS_CFG)
  #ADD_LIBRARY(grant STATIC ${LIB_SRC} ./src/wmachine.c)
ENDIF ()

IF (${ASSERT_NOT_CORE})
  ADD_DEFINITIONS(-DASSERT_NOT_CORE)
ENDIF (${ASSERT_NOT_CORE})

IF (TD_PRODUCT_NAME)
  ADD_DEFINITIONS(-DTD_PRODUCT_NAME="${TD_PRODUCT_NAME}")
ENDIF (${TD_PRODUCT_NAME})

IF (${TD_INDUSTRY})
  ADD_DEFINITIONS(-DTD_INDUSTRY)
  IF (TD_INDUSTRY_NAME)
    ADD_DEFINITIONS(-DTD_INDUSTRY_NAME="${TD_INDUSTRY_NAME}")
  ENDIF (${TD_INDUSTRY_NAME})
  IF (TD_FUNC_STREAM)
    ADD_DEFINITIONS(-DTD_FUNC_STREAM)
  ENDIF (${TD_FUNC_STREAM})
  IF (TD_FUNC_SUBSCRIPTION)
    ADD_DEFINITIONS(-DTD_FUNC_SUBSCRIPTION)
  ENDIF (${TD_FUNC_SUBSCRIPTION})
  IF (TD_FUNC_AUDIT)
    ADD_DEFINITIONS(-DTD_FUNC_AUDIT)
  ENDIF (${TD_FUNC_AUDIT})
  IF (TD_FUNC_CSV)
    ADD_DEFINITIONS(-DTD_FUNC_CSV)
  ENDIF (${TD_FUNC_CSV})
  IF (TD_FUNC_MULTI_TIER_STORAGE)
    ADD_DEFINITIONS(-DTD_FUNC_MULTI_TIER_STORAGE)
  ENDIF (${TD_FUNC_MULTI_TIER_STORAGE})
  IF (TD_FUNC_DATA_BAK_RESTORE)
    ADD_DEFINITIONS(-DTD_FUNC_DATA_BAK_RESTORE)
  ENDIF (${TD_FUNC_DATA_BAK_RESTORE})
  IF (TD_FUNC_OBJECT_STORAGE)
    ADD_DEFINITIONS(-DTD_FUNC_OBJECT_STORAGE)
  ENDIF (${TD_FUNC_OBJECT_STORAGE})
  IF (TD_FUNC_ACTIVE_ACTIVE)
    ADD_DEFINITIONS(-DTD_FUNC_ACTIVE_ACTIVE)
  ENDIF (${TD_FUNC_ACTIVE_ACTIVE})
  IF (TD_FUNC_DUAL_REPLICA_HA)
    ADD_DEFINITIONS(-DTD_FUNC_DUAL_REPLICA_HA)
  ENDIF (${TD_FUNC_DUAL_REPLICA_HA})
  IF (TD_FUNC_DB_ENCRYPTION)
    ADD_DEFINITIONS(-DTD_FUNC_DB_ENCRYPTION)
  ENDIF (${TD_FUNC_DB_ENCRYPTION})
  IF (TD_DATAIN_OPC_DA)
    ADD_DEFINITIONS(-DTD_DATAIN_OPC_DA)
  ENDIF (${TD_DATAIN_OPC_DA})
  IF (TD_DATAIN_OPC_UA)
    ADD_DEFINITIONS(-DTD_DATAIN_OPC_UA)
  ENDIF (${TD_DATAIN_OPC_UA})
  IF (TD_DATAIN_PI)
    ADD_DEFINITIONS(-DTD_DATAIN_PI)
  ENDIF (${TD_DATAIN_PI})
  IF (TD_DATAIN_KAFKA)
    ADD_DEFINITIONS(-DTD_DATAIN_KAFKA)
  ENDIF (${TD_DATAIN_KAFKA})
  IF (TD_DATAIN_INFLUXDB)
    ADD_DEFINITIONS(-DTD_DATAIN_INFLUXDB)
  ENDIF (${TD_DATAIN_INFLUXDB})
  IF (TD_DATAIN_MQTT)
    ADD_DEFINITIONS(-DTD_DATAIN_MQTT)
  ENDIF (${TD_DATAIN_MQTT})
  IF (TD_DATAIN_AVEVAHISTORIAN)
    ADD_DEFINITIONS(-DTD_DATAIN_AVEVAHISTORIAN)
  ENDIF (${TD_DATAIN_AVEVAHISTORIAN})
  IF (TD_DATAIN_OPENTSDB)
    ADD_DEFINITIONS(-DTD_DATAIN_OPENTSDB)
  ENDIF (${TD_DATAIN_OPENTSDB})
  IF (TD_DATAIN_TDengine_2_6)
    ADD_DEFINITIONS(-DTD_DATAIN_TDengine_2_6)
  ENDIF (${TD_DATAIN_TDengine_2_6})
  IF (TD_DATAIN_TDengine_3_0)
    ADD_DEFINITIONS(-DTD_DATAIN_TDengine_3_0)
  ENDIF (${TD_DATAIN_TDengine_3_0})
  IF (TD_DATAIN_MYSQL)
    ADD_DEFINITIONS(-DTD_DATAIN_MYSQL)
  ENDIF (${TD_DATAIN_MYSQL})
  IF (TD_DATAIN_POSTGRES)
    ADD_DEFINITIONS(-DTD_DATAIN_POSTGRES)
  ENDIF (${TD_DATAIN_POSTGRES})
  IF (TD_DATAIN_ORACLE)
    ADD_DEFINITIONS(-DTD_DATAIN_ORACLE)
  ENDIF (${TD_DATAIN_ORACLE})
ENDIF (${TD_INDUSTRY})

IF(TD_ENTERPRISE)
  IF (TD_LINUX_64)
    IF (TD_NINGSI)
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_ningsi.o)
      ELSEIF (TD_ARM_64)  
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_arm64.o)
    ELSEIF (TD_ARM_32)   
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_arm32.o)
    ELSEIF (TD_ALPINE)
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_alpine64.o)
    ELSEIF (TD_MIPS_64)
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_mips64.o)
    ELSE ()
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_linux64.o)
    ENDIF ()
  ELSEIF (TD_DARWIN_64)
    IF (TD_ARM_64)
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_darwin_m1.o)
    ELSE ()
      ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_darwin.o)
    ENDIF ()
  ELSEIF (TD_ARM_64)  
    ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_arm64.o)
  ELSEIF (TD_ARM_32)  
    ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_arm32.o)
  ELSEIF (TD_WINDOWS_64)  
    ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_windows64.o)
  ELSEIF (TD_WINDOWS_32)  
    ADD_LIBRARY(grant STATIC ${LIB_SRC} ${TD_SOURCE_DIR}/enterprise/contrib/grant-lib/machine_windows32.o)
  ELSEIF (TD_DARWIN)  
    ADD_LIBRARY(grant STATIC ${LIB_SRC} ./src/wmachine.c)
  ELSE()
    MESSAGE (FATAL_ERROR "grant-lib error: unknown platform")
  ENDIF ()
ENDIF ()

target_link_libraries(grant util sdb mnode)