name: Release workflows for TDengine
type: workflow

tasks:
  - task: td_release_cfg/lite.yaml
  - task: td_release_cfg/community.yaml
  - task: td_release_cfg/enterprise.yaml
  - task: oem_release_cfg/oem.yaml

