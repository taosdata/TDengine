settings:
  - name: taosd
    fqdn: [vm130]
    spec:
      version: 2.4.0.0  # 默认最新版
      config: # common config for all dnodes
        firstEP: vm130:6030
      dnodes:
        - endpoint: vm130:6030
          config_dir: /run/dnode1/config
          config: # will write into taos.cfg
            dataDir: /run/dnode1/data
            logDir: /run/dnode1/log
            debugFlag: 135
        - endpoint: vm130:6130
          config_dir: /run/dnode2/config
          config: # will write into taos.cfg
            dataDir: /run/dnode2/data
            logDir: /run/dnode2/log
            debugFlag: 135
        - endpoint: vm130:6230
          config_dir: /run/dnode3/config
          config: # will write into taos.cfg
            dataDir: /run/dnode3/data
            logDir: /run/dnode3/log
            debugFlag: 135
        - endpoint: vm130:6330
          config_dir: /run/dnode4/config
          config: # will write into taos.cfg
            dataDir: /run/dnode4/data
            logDir: /run/dnode4/log
            debugFlag: 135
        - endpoint: vm130:6430
          config_dir: /run/dnode5/config
          config: # will write into taos.cfg
            dataDir: /run/dnode5/data
            logDir: /run/dnode5/log
            debugFlag: 135

  - name: taospy
    fqdn: [vm130]
    spec:
      version: 2.2.0  
      client_version: 2.4.0.0
      config:
        firstEP: vm130:6030
        logDir: /run/dnode1/log
        debugFlag: 135

