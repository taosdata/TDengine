# 数据源 DSN 规则优化 - Test Spec

## 1. 测试目标

这里用于描述本需求主要的测试目标
- 优化数据源DSN的规则, 以便可以正确区分 DSN 是数据库、数据表(含超级表、子表、普通表)或者topic

## 2. 变更历史

| Date | Version | Owner | Memo |
| --- | --- | --- | --- |
| 2024-09-18 | 0.1 | 宋正勤 |  |
|  |  |  |  |

## 3. 测试范围

这里用于描述本需求的覆盖范围：
- 主要考虑涉及DSN的所有区域：
   - 数据同步任务
   - 数据订阅覆盖所有支持类型的DSN
   - TDengine 3.x数据源任务
   - TDengine 2.x数据源任务
   - 同时考虑原生连接taos和websocket连接
   - explorer没提供的可以用命令行覆盖

## 4. 测试结论

测试结论中包含结论和关键数据，但不需罗列过多细节，此处需要把把握信息的详细程度，原则上是外部 Reviewer 能够获得清晰的测试结论且尽量没有冗余信息为标准（这个标准是一句正确的废话，具体实行中需要大家 case by case 来处理）

## 5. 开发质量报告

结论：本特性/优化的开发质量是（优，良，一般，差，很差）

| 统计指标 | 数量 |
| --- | --- |
| 提测被拒次数 |  |
| 基础测试用例不通过 |  |
| Bug 总数 |  |
| 严重 Bug 总数 |  |

## 6. 已知问题和限制

这里用于记录产品使用上的一些限制，包括不支持的场景等，以及在发版时没有解决的minor issues.

## 7. 测试环境

## 8. 测试数据 (Optional)

这里用于描述性能、稳定性测试时的数据准备工作，包括但不局限于：

## 9. 测试用例

### 9.1 功能

在提测时，开发应保证基础用例全部通过。
| 任务分类 | 类型 | 测试目的 | 测试步骤 | 预期结果 | 是否为基础用例 | 测试结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 数据订阅 | （类型： 数据库） | 可以成功创建此主题，并且能正确工作 | 通过explorer在数据订阅，添加主题，类型选择“数据库” | 1. 订阅能创建成功
1. 此订阅DSN应为 /topic:topicName |  |  |  |
|  | （类型： 超级表） | 可以成功创建此主题，并且能正确工作 | 通过explorer在数据订阅，添加主题，类型选择“超级表” | 1. 订阅能创建成功
1. 此订阅的DSN应为 /topic:topicName |  |  |  |
|  | （类型： 子查询-> 超级表） | 可以成功创建此主题，并且能正确工作 | 通过explorer在数据订阅，添加主题，类型选择“ 子查询-> 超级表” | 1. 订阅能创建成功
1. 此订阅的DSN应为 /topic:topicName |  |  |  |
|  | （类型： 子查询-> 表 ） | 可以成功创建此主题，并且能正确工作 | 通过explorer在数据订阅，添加主题，类型选择“ 子查询-> 表” | 1. 订阅能创建成功
1. 此订阅的DSN应为  /topic:topicName |  |  |  |
| 数据源任务 | 企业版本地数据写入任务 | 分别使用上面“数据订阅”4个用例创建的主题作为Topic DSN, 可以成功同步数据到Taosd | 1. 复制上面“数据订阅”4个用例创建的主题作为Topic DSN
1. 创建TDengine 3.x数据源写入任务 | 1. 数据写入任务可以成功创建
2. 期望的数据可以同步写入Taosd |  |  |  |
|  | 云服务 到 TDengine 3.x | 云服务上的数据可以同步写入企业版 Taosd | 1. 在云服务上通过数据订阅创建主题，并复制此Topic DSN
1. 在本地企业版上使用此 Topic DSN 创建TDengine 3.x数据源写入任务 | 云服务上的数据可以成功写入本地Taosd |  |  |  |
|  | TDengine 3.x 到 云服务 | 企业版 Taosd上的数据可以导入云服务上 | 1. 在企业版上通过数据订阅创建主题，并复制此Topic DSN
1. 在云服务上使用此 Topic DSN 创建TDengine 3.x数据源写入任务 | 企业版 Taosd上的数据可以导入云服务上 |  |  |  |
| 数据同步 | 直接创建 | 可以成功创建此同步，并且能正确工作 | 通过explorer在数据同步，添加新的数据同步，源选择 源库, 目标库使用taos+ws | 1. 此同步能创建成功
1. 此同步能禁用
2. 此同步能删除 |  |  |  |
|  | 先创建主题，再创建同步 | 先创建名为A的主题，再创建源库为A同步 | 1. 先创建名为A的主题
1. 再创建源库为A同步 | 1. 此同步能创建成功
2. 此同步能禁用
3. 此同步能删除 |  |  |  |
|  | 先创建无关的主题db:A (db:A订阅的是别的库B)， 再用库A创建一个数据同步 | 测试数据同步在数据同步任务之前，如果存在无关的同名订阅主题，应该不能正常工作 | 1. 先创建名为db:A的主题，但是该主题订阅的是库B
1. 再用库A创建一个数据同步 | 应该报错 |  |  |  |
|  | 从  云服务  到 TDengine 3.x | 测试可以将云服务上的数据通过数据同步到本地的taosd | 1. 先本地企业版上创建数据订阅的主题，并记录此Topic DSN
1. 在云服务上使用此Topic DSN创建数据同步任务 | 同步任务可以创建成功，并能正确同步数据 |  |  |  |
|  | 从 TDengine 3.x  到 云服务 | 测试可以将本地taosd的数据通过数据同步到云服务 | 1. 先在云服务上创建数据订阅的主题，并记录此Topic DSN
1. 在本地企业版使用此Topic DSN创建数据同步任务 | 同步任务可以创建成功，并能正确同步数据 |  |  |  |
| TDengine 升级 | 数据订阅 | 验证升级以后的数据订阅功能可以正确工作 | 1. 先在老版本上创建一个数据订阅的主题
1. 创建类型为TDengine 3.x 数据源任务
2. 升级TDengine | 升级前和升级后此数据源任务都可以正确工作 |  |  |  |
|  | 数据同步 | 验证升级以后的数据同步功能可以正确工作 | 1. 先在老版本上创建一个数据同步任务
1. 升级TDengine | 升级前和升级后此数据同步任务都可以正确工作 |  |  |  |

### 9.2 可用性

测试用例包括但不局限于：
- UI是否美观？
- 交互是否合理？
- 字体、字号是否合适？
- 是否存在错别字？

### 9.3 可靠性

这里用于描述稳定性测试相关的内容。

### 9.4 性能

这里用于描述性能测试相关的内容。

### 9.5 安全性

测试用例包括但不局限于：
- 日志中是否包含敏感信息？

### 9.6 兼容性

测试用例包括但不局限于：
- 升级安装后，老版本（上一个版本）下创建的任务，能否继续执行？

### 9.7 本地化

测试用例包括但不局限于：
- 点击切换语言按钮后，UI上的所有元素是否按照选择的语言，正确展示？

## 10. 待讨论(Optional)

这里用于记录在测试或用例编写过程中想到的需要讨论的问题：

## 11. Jira

此feature相关的所有Jira, 标题中应包含统一的标签: abc

## 12. 测试计划 (Optional)

这里用于计划此 feature 测试的开始和结束时间。

## 13. 风险评估

用户记录这个需求的潜在风险，例如：对于功能复杂，开发时间长的功能，是否需要分期提测？

## 14. 测试备忘 (Optional)

这里用于记录测试过程中发现的，与产品行为相关的一些重要信息。

## 15. 参考文档 (Optional)

这里用于添加对该需求测试有帮助的文档链接：
- [数据源 DSN 规则优化](https://taosdata.feishu.cn/wiki/W68Hwt60eiinFTk9xIKcez4LnMe)
- 
  TD-31841
