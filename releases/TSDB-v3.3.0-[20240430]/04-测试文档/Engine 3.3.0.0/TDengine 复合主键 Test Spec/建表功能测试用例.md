# 建表功能测试用例

### 1. 建表功能测试用例

| 测试类型 | 测试场景 | 用例No. | 测试用例名称 | 数据准备 | 测试步骤 | 测试结果 | automated | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 定义复合主键Timestamp + int32 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, data1 FLOAT, data2 int ) tags (engineID int); | pass | Y |  |
| 2 | 定义复合主键Timestamp + int64 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id bigint PRIMARY KEY, data1 FLOAT, data2 int )tags (engineID int); | pass | Y |  |
| 3 | 定义复合主键Timestamp + uint32 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id int UNSIGNED PRIMARY KEY, data1 FLOAT, data2 int )tags (engineID int); | pass | Y |  |
| 4 | 定义复合主键Timestamp + uint64 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id bigint unsigned primary KEY, data1 FLOAT, data2 int )tags (engineID int); | pass | Y |  |
| 5 | 定义复合主键Timestamp + varchar |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY, data1 FLOAT, data2 int )tags (engineID int); | pass | Y |  |
| 6 | 定义复合主键Timestamp + binary |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id binary(10) PRIMARY KEY, data1 FLOAT, data2 int )tags (engineID int); | pass | Y |  |
| 基于带复合主键的超级表创建子表 | 7 | 基于带复合主键的超级表创建子表 |  | 1. 创建带复合主键的超级表 CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, data1 FLOAT, data2 int ) tags (engineID int); 1. 创建其子表 Create table c_t using t tags (1); 1. 通过describe查询子表schema，包含复合主键定义信息 | pass | Y |  |
| 基于带复合主键的超级表批量创建子表 | 8 | 基于带复合主键的超级表批量创建子表 |  | CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, data1 FLOAT, data2 int ) tags (engineID int); 2. 批量创建其子表 Create table c_t using t tags (1) c_t2 using t tags (2) c_t3 using t tags (3); 1. 通过describe查询各个子表schema，包含复合主键定义信息 | pass | Y |  |
| 9 | 定义复合主键Timestamp + int32 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 10 | 定义复合主键Timestamp + int64 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id bigint PRIMARY KEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 11 | 定义复合主键Timestamp + uint32 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id int UNSIGNED PRIMARY KEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 12 | 定义复合主键Timestamp + uint64 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id bigint unsigned primary KEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 13 | 定义复合主键Timestamp + varchar |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 14 | 定义复合主键Timestamp + binary |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id binary(10) PRIMARY KEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 15 | 超级复合主键关键字大小混写 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PriMary Key, data1 FLOAT, data2 int ) tags (id varchar(5)); | pass | Y |  |
| 16 | 普通表复合主键关键字大小混写 |  | 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PriMary Key, data1 FLOAT, data2 int ); | pass | Y |  |
| 17 | 超级表缺少关键字KEY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 18 | 普通表缺少关键字KEY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY , data1 FLOAT, data2 int ); | pass | Y |  |
| 19 | 超级表缺少关键字PRIMARY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) Key , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 20 | 普通表缺少关键字PRIMARY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) Key , data1 FLOAT, data2 int ); | pass | Y |  |
| 21 | 超级表关键字PRIMARY拼写错误 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIRY KEY , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 22 | 普通表关键字PRIMARY拼写错误 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIRY KEY , data1 FLOAT, data2 int ); | pass | Y |  |
| 23 | 超级表关键字KEY拼写错误 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 24 | 普通表关键字KEY拼写错误 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 25 | 超级表关键字PRIMARY和KEY中间缺少空格 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARYKEY , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 26 | 普通表关键字PRIMARY和KEY中间缺少空格 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARYKEY, data1 FLOAT, data2 int ); | pass | Y |  |
| 27 | 超级表只有ts和复合主键 |  | 创建包含复合主键的table成功 CREATE TABLE t ( ts TIMESTAMP, obj_id nchar(10) PRIMARY KEY ) tags ( name nchar(10)); | pass | Y |  |
| 28 | 普通表只有ts和复合主键 |  | 创建包含复合主键的table成功 CREATE TABLE t ( ts TIMESTAMP, obj_id nchar(10) PRIMARY KEY ) ; | pass | Y |  |
| 29 | 超级表复合主键列为TIMESTAMP |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id TIMESTAMP PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 30 | 普通表复合主键列为TIMESTAMP |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id TIMESTAMP PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 31 | 超级表复合主键列为FLOAT |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id FLOAT PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 32 | 普通表复合主键列为FLOAT |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id FLOAT PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 33 | 超级表复合主键列为DOUBLE |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id DOUBLE PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 34 | 普通表复合主键列为DOUBLE |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id DOUBLE PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 35 | 超级表复合主键列为SMALLINT |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id SMALLINT PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 36 | 普通表复合主键列为SMALLINT |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id SMALLINT PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 37 | 超级表复合主键列为SMALLINT UNSIGNED |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id SMALLINT UNSIGNED PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 38 | 普通表复合主键列为SMALLINT UNSIGNED |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id SMALLINT UNSIGNED PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 39 | 超级表复合主键列为TINYINT |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id TINYINT PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 40 | 普通表复合主键列为TINYINT |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id TINYINT PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 41 | 超级表复合主键列为TINYINT UNSIGNED |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id TINYINT UNSIGNED PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 42 | 普通表复合主键列为TINYINT UNSIGNED |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id TINYINT UNSIGNED PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 43 | 超级表复合主键列为BOOL |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id bool PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 44 | 普通表复合主键列为BOOL |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id bool PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 45 | 超级表复合主键列为 JSON |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id JSON PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 46 | 普通表复合主键列为 JSON |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id JSON PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 47 | 超级表复合主键列为 GEOMETRY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id GEOMETRY PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 48 | 普通表复合主键列为 GEOMETRY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id GEOMETRY PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 49 | 超级表复合主键列为 VARBINARY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id VARBINARY(10) PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 50 | 普通表复合主键列为 VARBINARY |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id VARBINARY(10) PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 51 | 超级表复合主键列为 NCHAR |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id NCHAR(10) PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 52 | 普通表复合主键列为 NCHAR |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id NCHAR(10) PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 53 | 超级表复合主键缺少数据类型 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id PRIMARY KEY1 , data1 FLOAT, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 54 | 普通表复合主键缺少数据类型 |  | 创建包含复合主键的table失败 CREATE TABLE t ( ts TIMESTAMP, obj_id PRIMARY KEY1 , data1 FLOAT, data2 int ); | pass | Y |  |
| 55 | 超级表2、3列为主键 |  | CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, obj_id2 int PRIMARY KEY, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 56 | 普通表2、3列为主键 |  | CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, obj_id2 int PRIMARY KEY, data2 int ) tags ( name nchar(10)); | pass | Y |  |
| 57 | 超级表2、4列为主键 |  | CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, data1 FLOAT, obj_id2 int PRIMARY KEY, data2 int ); | pass | Y |  |
| 58 | 普通表2、4列为主键 |  | CREATE TABLE t ( ts TIMESTAMP, obj_id int PRIMARY KEY, data1 FLOAT, obj_id2 int PRIMARY KEY, data2 int ); | pass | Y |  |
| 59 | 超级表复合主键列不支持修改操作 |  | 1. 创建超级表 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY, data1 FLOAT, data2 int ) tags (id binary(10)); 1. 修改复合主键列宽报错 Alter table t modify column obj_id varchar(20); 1. 修改复合主键列名报错 Alter table t column obj_id new_obj_id; | pass | Y |  |
| 60 | 超级表复合主键列不支持增加操作 |  | 1. 创建超级表 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, data1 FLOAT, data2 int ) tags (id binary(10)); 1. 增加复合主键列报错 Alter table t add column obj_id varchar(10); | pass | Y |  |
| 61 | 超级表复合主键列不支持删除操作 |  | 1. 创建超级表 成功创建包含复合主键的table CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY, data1 FLOAT, data2 int ) tags (id binary(10)); 1. 删除复合主键列报错 Alter table t drop column obj_id; | pass | Y |  |
| 62 | 普通表复合主键列不支持修改操作 |  | 1. 创建普通表 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY, data1 FLOAT, data2 int ); 1. 修改复合主键列宽报错 Alter table t modify column obj_id varchar(20); 1. 修改复合主键列名报错 Alter table t column obj_id new_obj_id; | pass | Y |  |
| 63 | 普通表复合主键列不支持增加操作 |  | 1. 创建普通表 CREATE TABLE t ( ts TIMESTAMP, data1 FLOAT, data2 int ); 1. 增加复合主键列报错 Alter table t add column obj_id varchar(10); | pass | Y |  |
| 64 | 普通表复合主键列不支持删除操作 |  | 1. 创建普通表 CREATE TABLE t ( ts TIMESTAMP, obj_id varchar(10) PRIMARY KEY, data1 FLOAT, data2 int ); 1. 删除复合主键列报错 Alter table t drop column obj_id; | pass | Y |  |
| 65 | 超级表PRIMARY KEY 列不是第二列 |  | 创建超级表报错 CREATE TABLE t ( ts TIMESTAMP, data1 FLOAT, obj_id varchar(10) PRIMARY KEY, data2 int ) tags (id binary(10)); | pass | Y |  |
| 66 | 普通表PRIMARY KEY 列不是第二列 |  | 创建超级表报错 CREATE TABLE t ( ts TIMESTAMP, data1 FLOAT, obj_id varchar(10) PRIMARY KEY, data2 int ) ; | pass | Y |  |
| 67 | 超级表 |  | 正确显示 primary key 关键字 | pass | Y |  |
| 68 | 普通表 |  | 正确显示 primary key 关键字 | pass | Y |  |
| 69 | 超级表 |  | 正确显示 primary key 关键字 | pass | Y |  |
| 70 | 普通表 |  | 正确显示 primary key 关键字 | pass | Y |  |
