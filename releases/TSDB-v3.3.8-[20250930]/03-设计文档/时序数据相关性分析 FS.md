# 时序数据相关性分析 FS

## 1. 背景

相关性是衡量两个序列之间关系的重要工具，可以帮助我们理解两个序列如何随时间共同变化，并揭示它们之间的同步性和动态性。
JIRA： [TD-37767](https://jira.taosdata.com:18080/browse/TD-37767)

## 2. 变更历史

| 日期 | 版本 | 负责人 | 主要修改内容 |
| --- | --- | --- | --- |
| 2025-09-12 | 1.0 | 廖浩均 | 初稿 |

## 3. 定义

**时间序列相关性分析：**检测两个或多个序列之间的统计关联，包括同步性、滞后性及因果性。相关性分析多用于以下的领域：
- 描述序列间的相似模式（如季节性的同步）
- 监控异常关联变化（如传感器数据突变）
- 描述一个时间序列对另一个时间序列的影响

**自相关‌（ACF）**：检测同一序列不同时间点数据的相关性，用于识别时间序列的周期性。
**皮尔逊相关系数（Pearson correlation coefficient）**：用于衡量两个时间序列之间线性相关程度的指标，其值介于 [-1, 1] 之间。
**时间滞后互相关（TLCC）**：评估两个时间序列之间的动态性关系，例如引导-追随关系。TLCC 通过逐步移动一个时间序列向量并反复计算两个信号间的相关性来测量。
**动态时间规整（DTW）**：计算两个时间序列间相关路径最短的方法，能够处理不同长度（采样间隔）的信号。可用于描述形状相似性（波动趋势）。

## 4. 行为说明

增加函数针对皮尔逊相关系数（Pearson）、动态时间规整(DTW)、时间滞后互相关(TLCC) 进行计算。

Corr 函数计算皮尔逊相关系数，返回值为 `double`， 返回结果在区间 [-1, 1] 内。只能对数值型列进行处理。
```sql
---  计算两列数据 col_1  和 col_2 的皮尔逊相关系数。
select corr(col_1, col_2) from foo 

---- 返回结果
0.431
```


dtw 函数计算两个输入列的动态时间规整结果值，返回值为 `double`。只能针对数值型列进行处理。
```sql
--- 前两个参数是两个输入列 
--- 第三个参数是计算序列点之间距离的度量方法。可选：1) 欧式距离 euclidean 和 2) 曼哈顿距离 manhattan
select dtw(col_1, col_2, 'euclidean') from foo

--- 返回结果
0.33
```


dtw_path 函数计算两个输入时间序列的动态时间规整配对结果（非规整结果值）, 以 varchar 形式返回（col_1， col_2 ）
```sql
--- 前两个参数是两个输入列 
--- 第三个参数是计算序列点之间距离的度量方法。可选：1) 欧式距离 euclidean 和 2) 曼哈顿距离 manhattan
--- 第四个参数指示返回结果是对应配对的 行号 (index) 或者 真实数值（value)
select dtw_path(col_1, col_2, 'euclidean', 'index') from foo

--- 返回结果
dtw_path
--------------
(0,0)
(0,1)
(1,2)
(2,3)
(3,4)
(4,5)
```


Tlcc 函数，计算不同时间滞后（时移）下的相关系数，量化一个序列相对于另一个序列的领先或滞后效应。返回值为 `double`， 只能针对数值型列进行处理。
```sql
---- 前两个参数是用于计算相关性的两列输入数据
---- 第三个参数是 col_2 相对于 col_1 的滞后点位数（阶数）, 可以为负值。
---- 第四个参数是 col_2 相对于 col_1 滞后点位数最大值（阶数上限）
---- 如果 lag_start == lag_end == 0，此时计算结果等于 corr(col_1, col_2)
select tlcc(col_1, col_2, lag_start, lag_end) from foo

---  返回结果
lag | corr
---------- 
0   | 0.1
1   |0.3
2   |0.6
3   |0.75
4   |0.8
5   |0.75
6   |0.6
7   |0.3
8   |0.1
```



## 5. 性能

Tlcc， dtw， dtw_path 依赖于 Python 库实现，全内存计算，性能可控。

## 6. 兼容性

无。

## 7. 运维

需要运维管理 TDgpt 服务正常运行

## 8. 使用场景

需要针对数据进行相关性评估的场景可以使用上述分析函数。
例如，针对数据库中的数据，需要进行预测分析，在使用协变量分析之前，可以使用 pearsonr 分析，确认数据协变量数据和主变量数据之间的有效性，以便于提供给时序基础模型进行预测分析。

## 9. 约束和限制

约束：
无
限制：
只能针对数值型列使用，使用 dtw 函数不能有数据缺失。
需要确保 TDgpt 正常运行状态

## 10. 常见错误和排查

对使用中可能遇到的错误提示以及如何排查故障进行说明。很多小型优化或功能不会有复杂的错误、排查也不困难，可以标无。但对于一些复杂的功能，容易用错、使用中容易出错的，要进行说明。

## 11. 可观测性

无

## 12. 安装和卸载

使用其中部分函数，需要搭配 TDgpt 提供的服务。因此需要 TDgpt 被正确部署并提供服务。

## 13. 文档

修改企业版文档
修改官网文档

## 14. 参考文档

https://blog.csdn.net/Eaton18/article/details/103116059
https://baike.baidu.com/item/%E6%A0%B7%E6%9C%AC%E7%9B%B8%E5%85%B3%E7%B3%BB%E6%95%B0/21884003
https://zhuanlan.zhihu.com/p/13650117056
[相关性分析 - RS](https://taosdata.feishu.cn/wiki/YB6vwoYKoifuEyko7YucUBS8n1f)

## 15. 附录

无
