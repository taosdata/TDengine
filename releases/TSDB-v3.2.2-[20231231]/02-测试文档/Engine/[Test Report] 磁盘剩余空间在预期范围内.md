# [Test Report] 磁盘剩余空间在预期范围内

测试方法：
     通过 0 级上同时向四块磁盘同时写入的方式，预期是四块磁盘中最先到达 2G 默认剩余空间的会报写入磁盘空间不足，重新写入后又能够继续向其它空闲磁盘写入，同时写入方式为 Round Robin 方式。
测试步骤：
  1）建立三节点集群，配置其中一个节点的 0 级为 四块磁盘，如下：
    
    
 2） 使用 TAOSBENCHMARK 写入，工具查询各个磁盘的写入情况，确认几个磁盘都有写入，如下：
     
同时写入重点在不断变化和切换，证明是 Round Robin 方式有效
     
    
 3）在最小的一块儿磁盘剩余空间小于 2G 后，TAOSBENCHMARK 报了写入失败，磁盘空间不足，如下，符合预期：
      
   查看磁盘，确实只剩下 1.9G了
    
4） 再启动 taosBenchmark 开始写入，预期是又可以正常写入，如下，没有报错：
    
5） 查询写入磁盘，小于 2G 的磁盘不再缩小，而其它几块大于 2G 剩余空间的仍然在写入数据，剩余空间在缩小：
    
  程序符合预期，测试通过，关闭
